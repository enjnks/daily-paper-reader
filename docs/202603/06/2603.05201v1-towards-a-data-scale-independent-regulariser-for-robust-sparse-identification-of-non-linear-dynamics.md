---
title: Towards a data-scale independent regulariser for robust sparse identification of non-linear dynamics
title_zh: 迈向一种用于非线性动力学鲁棒稀疏识别的数据尺度无关正则化项
authors: "Jay Raut, Daniel N. Wilke, Stephan Schmidt"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05201v1"
tags: ["keyword:SR", "query:SR"]
score: 9.0
evidence: 用于发现非线性动力学控制方程的新型稀疏回归算法
tldr: 针对数据归一化会扭曲非线性动力学稀疏识别（SINDy）结果的问题，本文提出了一种对数据缩放具有鲁棒性的新算法STCV。该方法放弃了传统的基于幅值的阈值筛选，转而采用无量纲的统计指标“系数存在度（CP）”来评估候选项的显著性。实验证明，STCV在处理带噪声的归一化数据时，能比传统方法更准确地识别出稀疏的物理规律，显著提升了模型的可解释性和可靠性。
motivation: 传统SINDy方法依赖幅值阈值，在数据归一化或存在噪声时容易导致识别出的物理模型过于复杂且不准确。
method: 提出STCV算法，利用变异系数和系数存在度（CP）这一统计指标代替幅值进行稀疏项筛选。
result: 在多个动力学系统和物理实验中，STCV在处理归一化噪声数据时的表现显著优于STLSQ和E-SINDy。
conclusion: STCV通过消除归一化的干扰，使稀疏系统识别成为一种更可靠、自动化的工具，增强了物理模型的可信度。
---

## 摘要
数据归一化是工程和科学应用中常见且通常必要的预处理步骤，但它会严重扭曲基于幅值的稀疏回归方法对控制方程的发现。这一问题在非线性动力学稀疏识别（SINDy）框架中尤为突出，其中稀疏性的核心假设受到数据缩放与测量噪声之间相互作用的破坏。由此发现的模型可能是稠密的、不可解释的，且在物理上是错误的。为了解决这一关键漏洞，我们引入了变异系数顺序阈值法（STCV），这是一种新型、计算高效的稀疏回归算法，对数据缩放具有内在的鲁棒性。STCV 用一种无量纲统计指标——系数存在度（CP）取代了传统的基于幅值的阈值处理，该指标用于评估模型库中候选项的统计有效性和一致性。这种从幅值到统计显著性的转变使得发现过程对任意数据缩放具有不变性。通过对典型动力系统和实际工程问题（包括物理质量-弹簧-阻尼器实验）的全面基准测试，我们证明了 STCV 在归一化且含噪声的数据集上始终且显著优于标准的顺序阈值最小二乘法（STLSQ）和集成 SINDy（E-SINDy）。结果表明，即使在其他方法失效的情况下，基于 STCV 的方法也能成功识别出正确的、稀疏的物理定律。通过减轻归一化的扭曲效应，STCV 使稀疏系统识别成为现实应用中更可靠、更自动化的工具，从而增强了模型的可解释性和可信度。

## Abstract
Data normalisation, a common and often necessary preprocessing step in engineering and scientific applications, can severely distort the discovery of governing equations by magnitudebased sparse regression methods. This issue is particularly acute for the Sparse Identification of Nonlinear Dynamics (SINDy) framework, where the core assumption of sparsity is undermined by the interaction between data scaling and measurement noise. The resulting discovered models can be dense, uninterpretable, and physically incorrect. To address this critical vulnerability, we introduce the Sequential Thresholding of Coefficient of Variation (STCV), a novel, computationally efficient sparse regression algorithm that is inherently robust to data scaling. STCV replaces conventional magnitude-based thresholding with a dimensionless statistical metric, the Coefficient Presence (CP), which assesses the statistical validity and consistency of candidate terms in the model library. This shift from magnitude to statistical significance makes the discovery process invariant to arbitrary data scaling. Through comprehensive benchmarking on canonical dynamical systems and practical engineering problems, including a physical mass-spring-damper experiment, we demonstrate that STCV consistently and significantly outperforms standard Sequential Thresholding Least Squares (STLSQ) and Ensemble-SINDy (E-SINDy) on normalised, noisy datasets. The results show that STCV-based methods can successfully identify the correct, sparse physical laws even when other methods fail. By mitigating the distorting effects of normalisation, STCV makes sparse system identification a more reliable and automated tool for real-world applications, thereby enhancing model interpretability and trustworthiness.

---

## 论文详细总结（自动生成）

这篇论文针对非线性动力学系统识别中的一个关键实际问题——数据缩放（归一化）对稀疏回归的干扰，提出了一种创新的解决方案。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在利用 SINDy（非线性动力学稀疏识别）框架发现物理方程时，通常需要对不同量级的观测数据进行归一化（如缩放到 [-1, 1]）以保证数值稳定性。然而，传统的稀疏回归算法（如 STLSQ）依赖于系数的**绝对幅值**进行筛选。
*   **研究动机**：在存在测量噪声的情况下，归一化会扭曲系数分布，导致噪声引起的虚假项系数可能比真实的物理项系数更大。这使得传统方法在处理归一化后的噪声数据时，往往无法识别出正确的稀疏模型，产生冗余且无物理意义的项。
*   **整体含义**：论文旨在开发一种**与数据尺度无关**的正则化方法，使物理规律的发现过程对数据预处理（缩放）具有鲁棒性。

### 2. 论文提出的方法论：STCV 算法
*   **核心思想**：放弃基于“幅值”的阈值筛选，转而采用基于**统计一致性**的指标。作者认为，真实的物理项在不同数据子集上的估计值应是稳定的，而噪声项则会剧烈波动。
*   **关键技术细节**：
    *   **变异系数（CV）**：定义为系数标准差与均值的比值。
    *   **系数存在度（CP, Coefficient Presence）**：这是核心指标，公式为 $CP_{ij} = \frac{\sqrt{m} \cdot \mu_{\xi_{ij}}}{\sigma_{\xi_{ij}}}$（其中 $m$ 是样本量）。CP 值越高，表示该项在统计上越显著。
    *   **贝叶斯线性回归（BLR）**：为了高效获取系数的均值和方差，STCV 放弃了高成本的蒙特卡洛采样（MCMC），采用了具有闭式解的 BLR，极大地提升了计算效率。
*   **算法流程**：
    1.  预处理数据并初始化模型。
    2.  使用 BLR 估计系数的后验分布。
    3.  计算各候选项的 CP 值。
    4.  **顺序阈值处理**：迭代剔除 CP 值低于设定阈值的项。
    5.  引入**超参数渐进策略**（Ramping Strategy）：类似于模拟退火，在迭代过程中逐渐减小岭回归惩罚并提高 CP 阈值，以稳定地收敛至稀疏解。

### 3. 实验设计
*   **数据集/场景**：
    *   **经典动力学系统**：Lorenz 系统、Rössler 吸引子、Van der Pol 振子、Duffing 振子。
    *   **工程模拟系统**：受损轴承仿真（高刚度、跨尺度数据）、半车模型（线性与非线性，高维状态空间）。
    *   **物理实验验证**：搭建了真实的质量-弹簧-阻尼物理实验台，采集加速度数据进行验证。
*   **Benchmark 与对比方法**：
    *   对比了标准的 **STLSQ**（顺序阈值最小二乘法）和 **E-SINDy**（集成 SINDy）。
    *   评估指标为**成功率**（在不同噪声水平下正确识别模型结构的比例）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 或 CPU 型号及训练时长。
*   **推断**：由于 SINDy 及其变体（包括基于 BLR 的 STCV）主要涉及矩阵运算和线性代数求解，其计算开销远低于深度学习模型。作者强调了 STCV 的计算高效性，通常在普通工作站或笔记本电脑上即可快速运行。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对每个数值模拟系统，作者进行了 **1000 次独立重复实验**（不同噪声实现），以获取统计意义上的成功率。
    *   进行了详尽的**超参数网格搜索**，确保对比方法（STLSQ, E-SINDy）均处于最优表现。
    *   包含了**偏置测试**（Bias Test），分析算法对线性项和非线性项是否存在选择偏好。
*   **充分性评价**：实验设计非常充分且客观。通过从简单玩具模型到复杂工程仿真，再到真实物理实验的层层递进，有力地证明了方法的通用性和实用性。

### 6. 主要结论与发现
*   **鲁棒性**：STCV 在归一化数据上的表现远超 STLSQ 和 E-SINDy。在其他方法因归一化+噪声而彻底失效的区域，STCV 仍能保持极高的识别成功率。
*   **尺度无关性**：实验证明 STCV 的识别结果对数据缩放具有不变性，解决了工程应用中必须归一化但又怕扭曲结果的矛盾。
*   **物理一致性**：在真实物理实验中，只有 STCV 成功识别出了符合物理常识的线性模型，而其他方法引入了大量错误的非线性项。

### 7. 优点（亮点）
*   **统计学视角**：将稀疏项筛选从“比大小”提升到了“看显著性”，更符合科学发现的逻辑。
*   **计算高效**：利用 BLR 的解析解代替 MCMC，使其能够处理高维系统。
*   **实用性强**：直接解决了 SINDy 在实际工程数据（跨尺度、高噪声）中落地的痛点。

### 8. 不足与局限
*   **噪声依赖性**：在完全无噪声的理想数据上，STCV 可能会因为数值误差产生的微小方差而误删项（文中提到可通过注入微量人工噪声解决）。
*   **超参数调节**：虽然 CP 阈值比幅值阈值更具统计意义，但仍需要根据具体问题进行一定程度的调节或网格搜索。
*   **导数依赖**：与标准 SINDy 一样，该方法仍依赖于高质量的数值微分，尽管可以结合弱形式（Weak-form）来改进，但本文未深入探讨两者的融合。

（完）
