---
title: "Adaptive SINDy: Residual Force System Identification Based UAV Disturbance Rejection"
title_zh: 自适应 SINDy：基于残余力系统辨识的无人机抗扰
authors: "Fawad Mehboob, Amir Atef Habel, Roohan Ahmed Khan, Mikhail Derevianchenko, Clement Fortin, Dzmitry Tsetserukou"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08863v1"
tags: ["keyword:SR", "query:SR"]
score: 9.0
evidence: SINDy是符号回归家族中用于识别控制方程的核心算法。
tldr: 针对无人机在复杂湍流环境下的稳定性挑战，本文提出了一种名为 Adaptive SINDy 的新型控制框架。该方法结合了稀疏非线性动力学识别（SINDy）与递归最小二乘（RLS）自适应控制，用于实时建模残余力并补偿风力干扰。通过在仿真和轻型无人机实机上的验证，该方法在复杂轨迹跟踪中表现出优于传统 PID 和 INDI 控制器的鲁棒性和精度，显著提升了无人机在动态环境下的抗干扰能力。
motivation: 传统控制方法难以应对高度非线性的风力干扰，而深度学习方法又缺乏可解释性和泛化能力。
method: 将数据驱动的 SINDy 系统识别技术与 RLS 自适应控制相结合，实现对无人机残余力系统的在线建模与补偿。
result: 在风速达 2m/s 的环境下，该方法在圆形和双纽线轨迹跟踪中分别实现了 12.2cm 和 17.6cm 的均方根误差，性能优于基准控制器。
conclusion: Adaptive SINDy 有效结合了数据驱动建模的灵活性与自适应控制的实时性，为轻型无人机在复杂气象条件下的稳定飞行提供了可靠方案。
---

## 摘要
无人机（UAV）在湍流环境中的稳定性和控制是一个备受关注的问题。由于风动力学的高度非线性，设计一种鲁棒的抗扰控制算法具有挑战性，且使用解析技术对动力学进行建模并不直接。虽然使用扰动观测器和经典自适应控制的传统技术已取得一定进展，但它们大多局限于相对简单的环境。另一方面，基于学习的方法越来越多地被用于残余力建模和抗扰；然而，其泛化能力和可解释性是一个令人担忧的因素。为此，我们提出了一种将基于非线性动力学稀疏辨识（SINDy）的数据驱动系统辨识与递归最小二乘（RLS）自适应控制相结合的新颖方法，以在湍流环境中适应并抑制风扰。我们在 Gazebo harmonic 环境以及风速高达 2 m/s（来自四个方向）的实际飞行中测试并验证了我们的方法，从而创建了一个高度动态且湍流的环境。自适应 SINDy 在多项轨迹跟踪误差指标上优于基准 PID 和 INDI 控制器，且未发生坠毁。在圆形和双纽线轨迹上，分别实现了低至 12.2 cm 和 17.6 cm 的均方根误差（RMSE），以及 13.7 cm 和 10.5 cm 的平均绝对误差（MAE）。验证是在极轻量级的 Crazyflie 无人机上进行的，环境高度动态，用于复杂轨迹跟踪。

## Abstract
The stability and control of Unmanned Aerial Vehicles (UAVs) in a turbulent environment is a matter of great concern. Devising a robust control algorithm to reject disturbances is challenging due to the highly nonlinear nature of wind dynamics, and modeling the dynamics using analytical techniques is not straightforward. While traditional techniques using disturbance observers and classical adaptive control have shown some progress, they are mostly limited to relatively non-complex environments. On the other hand, learning based approaches are increasingly being used for modeling of residual forces and disturbance rejection; however, their generalization and interpretability is a factor of concern. To this end, we propose a novel integration of data-driven system identification using Sparse Identification of Non-Linear Dynamics (SINDy) with a Recursive Least Square (RLS) adaptive control to adapt and reject wind disturbances in a turbulent environment. We tested and validated our approach on Gazebo harmonic environment and on real flights with wind speeds of up to 2 m/s from four directions, creating a highly dynamic and turbulent environment. Adaptive SINDy outperformed the baseline PID and INDI controllers on several trajectory tracking error metrics without crashing. A root mean square error (RMSE) of up to 12.2 cm and 17.6 cm, and a mean absolute error (MAE) of 13.7 cm and 10.5 cm were achieved on circular and lemniscate trajectories, respectively. The validation was performed on a very lightweight Crazyflie drone under a highly dynamic environment for complex trajectory tracking.

---

## 论文详细总结（自动生成）

以下是对论文《Adaptive SINDy: Residual Force System Identification Based UAV Disturbance Rejection》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：无人机（UAV）在强风和湍流环境中的飞行稳定性是关键挑战。风力动力学具有高度非线性和不确定性，传统的解析建模（如扰动观测器）难以捕捉复杂动态，而深度学习方法（如神经网络）虽然强大，但往往缺乏可解释性，且需要海量训练数据，泛化能力受限。
*   **核心问题**：如何在保证模型可解释性和数据效率的前提下，实时识别并补偿由风力引起的“残余力”（Residual Forces），从而提升无人机在恶劣环境下的轨迹跟踪精度。

### 2. 论文提出的方法论
*   **核心思想**：将**稀疏非线性动力学识别（SINDy）**与**递归最小二乘（RLS）自适应控制**相结合。利用 SINDy 发现描述残余力的稀疏数学表达式，并通过 RLS 在线更新这些表达式的系数以适应动态变化的环境。
*   **关键技术细节**：
    *   **残余力建模**：将系统动力学分为已知部分（名义模型）和未知部分（风扰引起的残余力）。目标向量定义为实际状态导数与已知模型预测值之差。
    *   **物理启发式函数库（Library Design）**：不同于通用的多项式库，本文根据无人机受风时的物理表现（如为了抵消风力而产生的倾斜角），构建了包含俯仰角($\theta$)、横滚角($\phi$)及其与推力($T$)加权的三角函数库。
    *   **稀疏优化（SR3）**：采用稀疏松弛正则化回归（SR3）算法，在保证模型拟合度的同时，通过正则化使不重要的项系数归零，获得精简且具有物理意义的模型。
    *   **自适应更新流程**：控制器计算期望加速度与实际测量加速度的偏差，利用 RLS 算法实时调整 SINDy 识别出的基函数系数，动态补偿扰动。

### 3. 实验设计
*   **实验场景**：
    1.  **仿真环境**：使用 Gazebo Harmonic 模拟器，分别结合 ArduPilot SITL 和 Crazyflie 固件。
    2.  **实机环境**：使用极轻量级的 Crazyflie 2.1 无人机，在由 4 个导管风扇组成的湍流阵列（风速达 2 m/s）中飞行。
*   **轨迹任务**：圆形（Circle）、双纽线（Lemniscate/Infinity）、螺旋线（Spiral）。
*   **对比方法（Benchmarks）**：
    *   **PID 控制器**：传统基准。
    *   **INDI（增量非线性动态逆）**：先进的抗扰控制方法。
    *   **ADAIML**：基于预训练神经网络（类似 Neural-Fly）的自适应控制方法。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号或训练时长。
*   **硬件平台**：实机实验使用了 Crazyflie 2.1，其搭载的是 **STM32F405** 微控制器（168MHz）。这暗示了 Adaptive SINDy 算法具有极高的计算效率，能够直接在嵌入式低功耗芯片上实时运行，无需依赖高性能 GPU。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真部分：每种控制器在每种轨迹上进行了 **10 次独立运行**，并统计了均值和标准差。
    *   实机部分：共进行了 **15 次成功飞行**（6次圆形、4次双纽线、5次螺旋线）。
*   **充分性评价**：实验设计较为充分。涵盖了从仿真到实机的迁移验证，对比了经典控制（PID）、先进控制（INDI）和前沿学习方法（ADAIML），并使用了 RMSE、MAE 和 P95（95分位误差）等多种客观指标。

### 6. 论文的主要结论与发现
*   **性能优越**：Adaptive SINDy 在所有复杂轨迹跟踪任务中均优于 PID 和 INDI。在实机实验中，PID 控制器在风扰下全部坠毁，而 Adaptive SINDy 保持了 100% 的任务完成率。
*   **精度指标**：在实机双纽线轨迹中实现了 **12.2 cm 的 RMSE**，在圆形轨迹中实现了 **13.7 cm 的 MAE**。
*   **泛化与鲁棒性**：该方法在不同平台（ArduPilot 和 Crazyflie）上均表现出良好的适应性，证明了其作为一种通用抗扰框架的潜力。

### 7. 优点
*   **可解释性强**：相比黑盒神经网络，SINDy 生成的是显式的数学表达式，便于分析物理机理。
*   **数据效率高**：SINDy 仅需少量飞行数据即可识别出动力学结构，适合在线学习。
*   **计算开销低**：算法轻量，能够实现在资源受限的微型无人机板载实时运行。
*   **物理启发**：函数库的设计结合了无人机飞行的先验知识，提高了识别的准确性。

### 8. 不足与局限
*   **函数库依赖**：SINDy 的效果高度依赖于预设函数库的完备性，如果扰动来源超出了函数库的描述范围，性能可能会下降。
*   **风速上限**：实验中的风速最高为 2 m/s，对于更极端的强风环境（如 10 m/s 以上），该方法的有效性尚待验证。
*   **参数调优**：RLS 和 SR3 涉及多个超参数（如学习率、稀疏权重等），在不同机型上的调优过程可能较为繁琐。

（完）
