---
title: "Manifold-Adapted Sparse RBF-SINDy: Unbiased Library Construction and Unsupervised Discovery of Dynamical States in Turbulent Wall Flows"
title_zh: 流形自适应稀疏 RBF-SINDy：壁面湍流中动力学状态的无偏函数库构建与无监督发现
authors: "Miguel Perez-Cuadrado, Giorgio Maria Cavallazzi, Alfredo Pinelli"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08327v1"
tags: ["keyword:SR", "query:SR"]
score: 9.0
evidence: 动力学稀疏识别 (SINDy) 是物理系统符号回归的核心方法。
tldr: 本研究提出了一种流形自适应的稀疏RBF-SINDy方法，旨在仅通过壁面压力和剪切应力测量值，无监督地识别壁面湍流的动力学状态。该方法通过弧长均匀重采样和引入马氏距离度量，纠正了传统稀疏识别中因POD谱衰减和时间采样不均导致的结构偏差。在低雷诺数湍流槽道流中的应用证明，该模型能准确捕捉条带稳定态和爆发不稳定性等核心结构，并重现系统的不变测度与动力学演化。
motivation: 传统的稀疏识别方法在处理湍流数据时，受POD谱陡峭衰减和时间采样偏差影响，难以准确表征过渡态动力学和相空间几何结构。
method: 采用流形自适应的RBF-SINDy框架，通过弧长重采样平衡时间尺度，并利用基于局部聚类协方差的马氏距离构建无偏的函数库。
result: 在最小湍流槽道流中成功识别出近壁循环的稳定条带与爆发阶段，模型预测能力达到了李雅普诺夫预测时限，并支持通过牛顿迭代寻找不变解。
conclusion: 该方法证明了仅利用壁面观测即可无监督地恢复湍流吸引子的动力学骨架，为复杂流体的降阶建模和状态发现提供了新工具。
---

## 摘要
壁面约束流动的湍流吸引子并非无结构的奇异集，而是包含一个由动力学上截然不同的状态构成的骨架，这些状态通过稀少的定向演化相连，其几何特征反映在相空间轨迹的不变测度中。我们表明，仅通过壁面测量（即壁面压力和壁面剪切应力），在没有物理标签或先验知识的情况下，只要用于识别动力学的数据驱动函数库遵循吸引子的内在几何结构而非本征正交分解（POD）表示的方差层级，就可以恢复这一骨架。标准的稀疏识别方法在构建函数库时会引入两种结构性偏差。首先，POD 谱的陡峭衰减导致 k-means 聚类中的欧几里得距离由主导模态主导，使基函数中心塌缩到低维子空间中，导致过渡动力学的表征效果较差。其次，湍流轨迹在准不变状态附近变慢，因此均匀时间采样会过度表征这些区域，而对快速过渡采样不足。通过按弧长均匀重新采样轨迹，并将欧几里得度量替换为源自局部聚类协方差的马氏度量，可以纠正这两种偏差。对该修正后的函数库进行单次稀疏回归即可得到简化模型。应用于低雷诺数下的最小湍流通道，无监督聚类揭示了近壁循环的两个阶段：稳定条纹状态和对应于流动相干结构骨架的爆发诱发不稳定性。该模型重现了不变测度，达到了 Lyapunov 预测时限，并提供了一个可微向量场，在该向量场上可以通过牛顿迭代定位不变解。

## Abstract
The turbulent attractor of wall bounded flows is not a structureless strange set but contains a skeleton of dynamically distinct states connected by rare directed transitions whose geometry is reflected in the invariant measure of the phase space trajectory. We show that this skeleton can be recovered from wall measurements alone, namely wall pressure and wall shear stress, without physical labels or prior knowledge, provided that the data driven function library used to identify the dynamics respects the intrinsic geometry of the attractor rather than the variance hierarchy of the POD representation. Standard sparse identification approaches introduce two structural biases during library construction. First, the steep decay of POD spectra causes Euclidean distances in k means clustering to be dominated by leading modes, collapsing basis function centres into a low dimensional subspace and leaving transitional dynamics poorly represented. Second, turbulent trajectories slow near quasi invariant states, so uniform time sampling over represents these regions and under samples rapid transitions. Both biases are corrected by resampling the trajectory uniformly in arc length and replacing the Euclidean metric with a Mahalanobis metric derived from the local cluster covariance. A single sparse regression on this corrected library yields a reduced model. Applied to a minimal turbulent channel at low Reynolds number, unsupervised clustering reveals two phases of the near wall cycle: stable streak states and burst initiating instabilities corresponding to the coherent structure skeleton of the flow. The model reproduces the invariant measure, reaches the Lyapunov predictability horizon and provides a differentiable vector field on which invariant solutions can be located by Newton iteration.

---

## 论文详细总结（自动生成）

这篇论文提出了一种改进的稀疏识别（SINDy）框架，旨在解决壁面湍流降阶建模中的结构性偏差问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何仅通过壁面测量数据（压力和剪切应力），在无监督的情况下准确识别并建模壁面湍流的动力学骨架（即“条带-爆发”循环）。
*   **研究背景**：传统的 SINDy 方法结合 POD（本征正交分解）在处理湍流时存在两个致命偏差：
    1.  **坐标偏差**：POD 谱能量衰减极快，导致 k-means 聚类在欧几里得度量下被前几个主导模态控制，忽略了携带关键过渡动力学信息的低方差模态。
    2.  **时间采样偏差**：湍流在准平衡态（如条带期）停留时间长，在爆发期演化极快。均匀时间采样会导致模型过度拟合慢速状态，而对快速过渡过程采样不足。
*   **整体含义**：通过修正这些偏差，使数据驱动的函数库能够尊重吸引子的内在几何结构（不变测度），从而实现对湍流动力学状态的无监督发现。

### 2. 方法论
论文提出了 **Manifold-Adapted Sparse RBF-SINDy** 框架，核心步骤如下：
*   **弧长重采样（Arc-length Resampling）**：将轨迹按空间弧长均匀重采样，而非按时间均匀采样。这有效地增加了快速过渡区域的权重，使经验测度逼近物理不变测度。
*   **马氏距离聚类（Mahalanobis-metric Clustering）**：在 k-means 聚类中，用马氏距离取代欧几里得距离。马氏距离利用局部协方差矩阵对距离进行缩放，使得聚类中心能均匀分布在全维度的潜在空间中，而非塌缩到高方差子空间。
*   **各向异性 RBF 函数库**：构建基于马氏度量的径向基函数（RBF）。这些基函数的等值面是椭球体，其形状和方向与吸引子的局部几何结构完全匹配。
*   **相干性加权中心分配**：根据聚类的动力学相干性（速度矢量的余弦相似度）和样本数量分配 RBF 中心，确保动力学复杂的区域获得更多覆盖。
*   **全局稀疏回归**：使用顺序阈值最小二乘法（STLS）在修正后的函数库上进行单次全局回归，得到连续、可微的常微分方程（ODE）。

### 3. 实验设计
*   **数据集**：使用 $Re_\tau = 180$ 的最小湍流通道流（Minimal Turbulent Channel）的直接数值模拟（DNS）数据。
*   **输入数据**：仅包含壁面压力 $P_{wall}$、流向剪切应力 $\tau_x$ 和展向剪切应力 $\tau_z$。
*   **Benchmark**：与原始 DNS 数据的统计特性、时间演化轨迹以及传统的“有偏”SINDy 方法进行对比。
*   **评估指标**：包括不变测度恢复（PDF 匹配）、李雅普诺夫谱（Lyapunov Spectrum）、短期预测相关性（Pearson $R^2$）以及预测时限。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号或训练时长。
*   **效率提示**：文中提到，该降阶模型（ROM）在线预测的速度比光谱法 DNS 快 $10^3$ 到 $10^5$ 倍。由于回归过程是线性的且使用了随机 SVD，计算成本相对较低。

### 5. 实验数量与充分性
*   **实验规模**：主要针对 $Re_\tau = 180$ 这一典型案例进行了深入分析。
*   **充分性评价**：
    *   **深度充分**：实验不仅验证了轨迹预测，还深入探讨了李雅普诺夫稳定性、不变测度恢复和相空间几何结构，这在同类论文中较为罕见。
    *   **广度局限**：未在更高雷诺数或不同几何形状的流动中进行测试。
    *   **客观性**：通过与 Lyapunov 预测时限（理论极限）的对比，客观地证明了模型已达到信息提取的上限。

### 6. 主要结论与发现
*   **无监督状态发现**：修正后的模型在没有任何物理标签的情况下，自动将聚类分为两类：高驻留/低相干的“条带态”和低驻留/高相干的“爆发态”，这与经典的近壁循环物理图像完全吻合。
*   **动力学保真度**：模型成功捕获了约 35 个正李雅普诺夫指数，证明其重现了湍流的稀疏混沌特性。
*   **预测极限**：模型的预测精度在李雅普诺夫时限（$t^* \approx 5t^+$）内保持极高，随后由于系统内在混沌性自然发散，证明模型已捕获了所有可提取的动力学信息。
*   **不变解定位**：由于模型是全局可微的，可以直接通过牛顿迭代法在学习到的向量场中寻找精确相干结构（ECS）。

### 7. 优点（亮点）
*   **几何感知**：首次系统性地识别并纠正了 SINDy 在处理 POD 空间时的度量偏差和采样偏差。
*   **物理一致性**：无需先验物理知识，仅凭几何修正就恢复了湍流的动力学骨架。
*   **数学优雅性**：生成的模型是全局光滑的 ODE，避免了分段局部模型（如 ql-ROM）在边界处的跳跃问题。
*   **实用性**：仅依赖壁面传感器数据，具有极强的工程应用潜力（如主动流动控制）。

### 8. 不足与局限
*   **雷诺数限制**：目前仅在最小流单元（Minimal Flow Unit）和低雷诺数下验证，在高雷诺数下，壁面信号与内部结构的解耦可能会增加建模难度。
*   **超参数依赖**：虽然 G-means 可以自动确定聚类数，但 RBF 的带宽选择和正则化参数仍需一定程度的调优。
*   **维度灾难风险**：如果保留的 POD 模态数量极大，马氏矩阵的存储和求逆计算量会显著增加。

（完）
