---
title: "Discovering Mechanistic Models of Neural Activity: System Identification in an in Silico Zebrafish"
title_zh: 发现神经活动的机制模型：计算机模拟斑马鱼中的系统辨识
authors: "Jan-Matthis Lueckmann, Viren Jain, Michał Januszewski"
date: 2026-02-04
pdf: "https://arxiv.org/pdf/2602.04492v1"
tags: ["keyword:SR", "query:SR"]
score: 8.0
evidence: 基于大模型的树搜索用于发现可解释的机制模型
tldr: 本研究针对神经回路机制建模中缺乏真实标准的问题，构建了一个基于斑马鱼幼虫神经力学模拟的数字孪生测试平台。研究采用基于大语言模型（LLM）的树搜索算法，实现了预测性模型的自动发现。结果表明，该方法在预测性能上显著优于传统基准，并强调了结构先验在实现模型可解释性和跨分布泛化中的关键作用，为AI驱动的科学发现提供了新范式。
motivation: 旨在解决神经科学中因缺乏真实标准而难以验证神经回路机制模型的问题。
method: 建立了一个斑马鱼数字孪生测试平台，并利用基于大语言模型的树搜索算法自主探索神经活动模型。
result: 发现该方法在预测精度上超越了现有基准，且结构先验对于模型的可解释性和泛化能力至关重要。
conclusion: 研究证明了结合结构先验的AI搜索能有效发现复杂的生物机制模型，为处理真实神经数据提供了重要指导。
---

## 摘要
构建神经环路的机制模型是神经科学的一个基本目标，但由于缺乏基准真相（ground truth），验证此类模型受到了限制。为了严格测试模型发现，我们利用幼年斑马鱼的神经机械模拟建立了一个计算机模拟（in silico）测试平台，将其作为透明的基准真相。我们发现，基于大语言模型（LLM）的树搜索能够自主发现预测模型，其性能显著优于现有的预测基准。以感觉驱动为条件对于准确的系统辨识是必要的，但并不充分，因为模型会利用统计捷径。结构先验被证明对于实现稳健的分布外泛化以及恢复可解释的机制模型至关重要。我们的见解为真实世界神经记录的建模提供了指导，并为人工智能驱动的科学发现提供了一个更广泛的范式。

## Abstract
Constructing mechanistic models of neural circuits is a fundamental goal of neuroscience, yet verifying such models is limited by the lack of ground truth. To rigorously test model discovery, we establish an in silico testbed using neuromechanical simulations of a larval zebrafish as a transparent ground truth. We find that LLM-based tree search autonomously discovers predictive models that significantly outperform established forecasting baselines. Conditioning on sensory drive is necessary but not sufficient for faithful system identification, as models exploit statistical shortcuts. Structural priors prove essential for enabling robust out-of-distribution generalization and recovery of interpretable mechanistic models. Our insights provide guidance for modeling real-world neural recordings and offer a broader template for AI-driven scientific discovery.

---

## 论文详细总结（自动生成）

这篇论文由 Google Research 的团队发表，探讨了如何利用人工智能（特别是大语言模型驱动的代码搜索）来发现神经系统的机制模型。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：神经科学的一个根本目标是理解神经环路如何处理信息并产生行为。然而，由于生物实验中缺乏“基准真相”（Ground Truth），研究者无法确定一个高预测精度的模型是真正捕捉到了生物机制，还是仅仅利用了数据的统计捷径（Statistical Shortcuts）。
*   **研究背景**：真实的神经记录存在噪声、观测变量不全、成像时间短等限制。为了填补这一“验证鸿沟”，论文提出了使用**计算机模拟（in silico）**的神经力学模型作为透明的基准真相，以此来严格评估模型发现策略的有效性。

### 2. 论文提出的方法论
*   **核心思想**：将神经模型的设计转化为一个自动化优化问题，利用大语言模型（LLM）在代码空间中进行启发式搜索，以发现能够准确预测神经活动的动力学方程。
*   **关键技术细节**：
    *   **in Silico 测试平台**：基于 `simZFish` 模拟器，模拟斑马鱼幼虫的身体力学、流体动力学及视觉驱动的神经环路（包含 36 个神经元单位，涵盖前顶盖 ePT、lPT 和命令神经元 nMLF/aHB）。
    *   **LLM 引导的树搜索**：采用 **PUCT（预测上置信度界限树搜索）** 算法，结合 **Gemini 3 Flash** 模型。LLM 作为“变异算子”，不断修改和进化 Python 代码（基于 JAX/Flax 框架）。
    *   **两种搜索模式**：
        1.  **无约束搜索（黑盒）**：仅以最小化预测误差为目标。
        2.  **结构约束搜索（灰盒）**：向 LLM 提供关于神经环路层级和稀疏连接性的先验知识（不提供具体权重）。
*   **公式定义**：
    *   系统辨识目标：$\min_{\theta} \sum_{t=1}^{H} \|\hat{a}_t - a_t\|$，其中 $\hat{a}_t = f_{\theta}(\hat{a}_{t-1}, x_t)$，$a_t$ 为神经活动，$x_t$ 为感觉输入。

### 3. 实验设计
*   **数据集/场景**：模拟斑马鱼在四种视觉刺激条件（双侧向外、向内及两种非对称运动）下的反应，产生 400,000 个样本（1ms 分辨率）。
*   **Benchmark 与对比方法**：
    *   **时间序列基准**：`nlinear`、`tide`、`tsmixer`（SOTA 预测模型）。
    *   **循环神经网络**：`deepar`（概率自回归网络）。
    *   **评估指标**：测试集 MAE（分布内）、留出集 MAE（分布外 OOD）、雅可比敏感性分析（静态机制恢复）、脉冲响应分析（动态机制恢复）。

### 4. 资源与算力
*   **算力使用**：
    *   使用单块 **NVIDIA T4 GPU** 进行模型训练。
    *   每个候选解决方案的运行时间限制为 **12 小时**。
    *   树搜索限制为最多 **500 个节点**（即 500 次代码迭代）。
    *   使用了 Gemini 3 Flash 作为后端 LLM 进行代码生成。
*   **优化策略**：为了提高效率，搜索过程中仅在 32 步的截断序列上训练，但在评估时扩展到 256 步。

### 5. 实验数量与充分性
*   **实验规模**：进行了两组完整的树搜索（无约束 vs. 结构约束），每组探索了 500 个模型节点。
*   **充分性与公平性**：
    *   论文对比了多种主流的时间序列预测架构，并设置了严格的分布外（OOD）测试。
    *   通过对前 50 个高分模型进行群体统计分析（中位数与四分位距），证明了结论的稳健性。
    *   实验设计考虑了感觉驱动的消融实验，证明了其对系统辨识的必要性。

### 6. 论文的主要结论与发现
1.  **感觉驱动是辨识的前提**：在没有感觉输入的情况下，即使是真实的机制模型也无法通过历史数据准确预测未来（系统会衰减），证明了纯自回归建模的局限性。
2.  **预测精度不等于机制恢复**：无约束的 AI 搜索能发现预测精度极高（超越 SOTA 基准）的模型，但这些模型在 OOD 刺激下表现崩溃，且内部逻辑与真实生物环路完全不符。
3.  **结构先验至关重要**：引入连接图谱（Connectome）先验后，AI 能够准确恢复神经元间的兴奋/抑制模式和动态响应特性，并实现极强的 OOD 泛化能力。
4.  **AI 发现的模型具有可解释性**：结构约束下的模型代码呈现出语义化的模块，能够直接对应生物学上的功能基元。

### 7. 优点（亮点）
*   **方法创新**：将 LLM 代码生成与神经科学系统辨识结合，实现了动力学方程的自动化发现。
*   **验证严谨**：利用数字孪生（in silico）解决了神经科学中长期存在的“缺乏标准真相”的验证难题。
*   **实用建议**：为真实世界的神经数据建模提出了三条具体建议（重新定义预测任务、优先考虑 OOD 指标、利用物理布线作为支架）。

### 8. 不足与局限
*   **模型简化**：`simZFish` 虽然精细，但仍是脊椎动物大脑的简化版，主要集中在视觉运动环路，未涵盖更复杂的认知功能。
*   **计算成本**：LLM 引导的树搜索计算强度大，扩展到更高维度的神经数据（如数万个神经元）可能面临扩展性挑战。
*   **观测假设**：实验假设神经系统是全观测且高信噪比的，而真实实验往往只能部分观测且噪声巨大。

（完）
