---
title: "GAME: Genetic Algorithms with Marginalised Ensembles for model-independent reconstruction of cosmological quantities"
title_zh: GAME：用于宇宙学量模型无关重构的边缘化集成遗传算法
authors: "Matteo Peronaci, Matteo Martinelli, Savvas Nesseris"
date: 2026-02-13
pdf: "https://arxiv.org/pdf/2602.12870v1"
tags: ["keyword:SR", "query:SR"]
score: 10.0
evidence: 用于宇宙学非参数符号回归的遗传算法
tldr: 本研究针对遗传算法（GA）在宇宙学非参数重建中易陷入局部最优且对导数敏感的问题，提出了 GAME 方法。该方法通过对 GA 配置集成进行加权平均，并结合拟合优度与光滑度定义权重，同时利用路径积分法估算误差。实验证明 GAME 能更稳健地重建哈勃参数 H(z) 及暗能量状态方程 w(z)，为未来宇宙学观测提供了强有力的模型独立分析工具。
motivation: 传统的遗传算法在处理涉及导数的非观测物理量时，容易受到随机性和超参数选择的影响而陷入局部最优。
method: 提出 GAME 方法，通过对 GA 配置集成进行加权平均，并结合路径积分与集成方差来分析和修正重建误差。
result: 在通用测试函数和宇宙年代学数据上的应用显示，该方法重建的暗能量状态方程与 ΛCDM 模型保持一致。
conclusion: GAME 方法显著增强了遗传算法的稳健性，使其成为未来高精度宇宙学巡天中区分不同物理模型的有力工具。
---

## 摘要
遗传算法（GA）是随机优化和非参数符号回归的强大工具，已在宇宙学中得到广泛应用。它们能够直接从数据点重构解析函数，而无需引入新的物理模型。该方法的一个局限性在于，虽然重构函数在复现数据点行为方面非常高效，但涉及导数的不可观测物理量对随机性、超参数以及 GA 获得的最佳拟合函数的选择特别敏感，这意味着算法存在陷入局部最小值的风险。在这项工作中，我们提出了一种用于解析函数重构的 GA 方法改进方案，即计算 GA 配置集成的加权平均值（GAME）。我们通过一个兼顾数据点拟合优度和所得函数平滑度的量来定义权重。我们还提出了一种实用方法，通过结合路径积分法和集成方差，来解析地估计并修正平均函数的误差。我们在通用测试函数上展示了 GAME 方法带来的改进。随后，我们将该新方法应用于使用宇宙计时器（Cosmic Chronometers）数据的哈勃率 $H(z)$ 非参数重构，并在假设平直 Friedmann-Lemaître-Robertson-Walker 背景和广义相对论的前提下，推导了相应的暗能量状态方程 $w(z)$。通过一致性检验，我们表明当前数据产生的结果与 $\Lambda$CDM 模型兼容，并且第四阶段（Stage IV）宇宙学巡天将使经 GAME 方法增强的 GA 成为区分不同模型的更具竞争力的工具。

## Abstract
Genetic Algorithms (GA) are a powerful tool for stochastic optimisation and non-parametric symbolic regression, already widely used in cosmology. They are capable of reconstructing analytical functions directly from data points without introducing new physical models. A limitation of this approach is that while the reconstructed function is very efficient at reproducing the behaviour of the data points, non-observable quantities involving derivatives are particularly sensitive to stochasticity, hyperparameters, and to the choice of the best-fit function obtained by the GA, which implies the risk of the algorithm getting stuck in a local minimum. In this work we propose an update to the GA methodology for the reconstruction of analytical functions that involves computing a weighted average of an ensemble of GA configurations (\texttt{GAME}). We define the weights via a quantity that accounts for both the goodness-of-fit of the points and the smoothness of the resulting function. We also present a practical method to analytically estimate and correct the errors on the averaged function by combining a path-integral approach with an ensemble variance. We demonstrate the improvement offered by \texttt{GAME} methodology on a generic test function. We then apply the new methodology to a non-parametric reconstruction of the Hubble rate $H(z)$ using Cosmic Chronometers data and, assuming a flat Friedmann-Lemaître-Robertson-Walker background and General Relativity, we infer the corresponding dark energy equation of state $w(z)$. Through consistency tests, we show that current data produces results compatible with $Λ$CDM, and that Stage IV cosmology surveys will allow GA reinforced with \texttt{GAME} methodology to become an even more competitive tool for discriminating between different models.

---

## 论文详细总结（自动生成）

这是一份关于论文《GAME: Genetic Algorithms with Marginalised Ensembles for model-independent reconstruction of cosmological quantities》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨的核心问题是如何在不依赖特定物理模型（如 $\Lambda$CDM）的前提下，从观测数据中稳健地重构宇宙学函数（如哈勃率 $H(z)$ 和暗能量状态方程 $w(z)$）。
*   **研究动机**：虽然遗传算法（GA）作为一种非参数符号回归工具已被广泛应用，但它存在两个主要缺陷：一是**随机性与局部最优**，不同的超参数配置或随机种子可能导致截然不同的重构结果；二是**导数敏感性**，GA 在拟合观测点时表现良好，但在计算涉及导数的物理量（如 $w(z)$）时，结果往往极不稳定且易受噪声干扰。
*   **整体含义**：作者提出了 **GAME**（边缘化集成遗传算法）框架，旨在通过集成学习的思想提升 GA 的稳健性，并提供更可靠的误差估计。

### 2. 方法论：核心思想与关键技术
GAME 的核心思想是从“寻找单一最佳函数”转向“对一组候选函数进行加权平均（边缘化）”。
*   **$\chi^2$ 边缘化处理**：在 GA 运行过程中，对加性或乘性偏移量（如 $H_0$）进行解析边缘化，使算法专注于重构函数的“形状”而非绝对数值，从而加速收敛。
*   **集成加权平均（Ensemble Averaging）**：运行 $N_{conf}$ 组不同超参数（语法、种子等）的 GA，获得一组最佳拟合函数 $f_j(x)$。
*   **正则化权重定义**：引入评估指标 $S_j(\lambda) = \chi^2_j + \lambda R_j$，其中 $R_j$ 是粗糙度惩罚项（二阶导数的平方积分）。通过 **L-curve 方法** 自动寻找拟合优度（$\chi^2$）与平滑度（$R$）之间的平衡点 $\lambda_{elbow}$，据此计算每个模型的权重。
*   **总误差估计**：结合了两种误差来源：
    1.  **路径积分误差（$\delta f_{PI}$）**：基于似然函数的统计测量误差。
    2.  **集成方差（$\sigma_{ens}$）**：反映不同超参数配置之间的随机波动。
    两者通过平方和开根号的方式组合成总不确定度 $\sigma_{tot}$。

### 3. 实验设计
*   **数据集/场景**：
    1.  **通用测试函数**：使用一个带有指数项的复杂解析函数生成模拟数据，验证算法捕捉特征的能力。
    2.  **真实数据（CC）**：使用宇宙年代学（Cosmic Chronometers）提供的 $H(z)$ 直接测量数据。
    3.  **未来巡天模拟（Stage IV）**：生成符合 Euclid 或 DESI 精度要求的 Mock 数据（15个点，2% 误差），进行预测性分析。
*   **Benchmark（基准）**：传统的“单一最佳拟合 GA”（即在所有配置中仅选取 $\chi^2$ 最小的那个函数）。
*   **对比维度**：函数重构的准确度、导数的稳定性、以及对 $\Lambda$CDM 模型的偏离程度。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件型号（如 GPU/CPU 核心数）或具体的训练时长。
*   **计算规模**：作者提到，受益于现代计算能力的提升，现在可以运行 $O(10^4)$ 到 $O(10^5)$ 代的演化，并同时并行处理数百个超参数配置。这比早期 GA 在宇宙学中的应用（通常仅几千代）有了显著的计算量增加。

### 5. 实验数量与充分性
*   **实验规模**：
    *   测试函数部分运行了 99 种配置。
    *   宇宙学重构部分运行了 150 种配置（15 种语法组合 $\times$ 10 个随机种子）。
    *   针对 CC 数据进行了“全样本”和“高质量子集”的对比消融实验。
*   **充分性评价**：实验设计较为充分。作者不仅验证了方法在已知函数上的表现，还考虑了不同物质密度先验（Planck 2018 和 DESI DR1）对结果的影响。通过 Mock 数据的预测分析，证明了该方法在未来高精度数据下的潜力，实验逻辑闭环且客观。

### 6. 主要结论与发现
*   **稳定性大幅提升**：在导数重构（$H'(z)$）方面，GAME 相比标准 GA 实现了约 **95%** 的精度提升（基于累积 $\tilde{\chi}^2$ 统计量）。
*   **宇宙学一致性**：利用当前 CC 数据重构的 $w(z)$ 在 $1\sigma$ 范围内与 $\Lambda$CDM（即 $w=-1$）完全兼容。
*   **当前约束能力**：在 $z=0$ 处，利用 DESI 先验得到的暗能量状态方程为 $w(0) = -0.986 \pm 0.132$。
*   **未来展望**：Stage IV 巡天数据将使 $w(0)$ 的测量精度提升约 2.6 倍，GAME 能够有效利用这些高精度数据且不引入模型偏差。

### 7. 优点（亮点）
*   **解决“过拟合”与“不稳定性”**：通过引入粗糙度惩罚和集成平均，有效抑制了 GA 容易产生的病态震荡。
*   **全解析误差带**：提供了一种结合统计误差和系统（配置）误差的解析计算方案，这在符号回归领域非常难得。
*   **模型无关性**：不预设 $w(z)$ 的参数化形式（如 CPL 方案），能够捕捉数据中可能存在的非标准物理特征。

### 8. 不足与局限
*   **高红移发散**：在 $z > 1.5$ 时，由于暗能量密度趋于零，重构的 $w(z)$ 误差带剧烈膨胀，这是物理定义导致的奇异性，非参数方法在此处约束力有限。
*   **假设依赖**：虽然重构过程是模型无关的，但推导 $w(z)$ 仍需假设平直 FLRW 背景和广义相对论（GR）框架。
*   **计算成本**：相比于高斯过程（GP），GAME 需要运行大量的 GA 演化，计算开销显著更高。

（完）
