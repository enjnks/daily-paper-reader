---
title: Interpretable Analytic Calabi-Yau Metrics via Symbolic Distillation
title_zh: 通过符号蒸馏获得的可解释解析卡拉比-丘度规
authors: D Yang Eng
date: 2026-02-08
pdf: "https://arxiv.org/pdf/2602.07834v1"
tags: ["keyword:SR", "query:SR"]
score: 10.0
evidence: 利用符号回归将神经网络近似蒸馏为可解释公式
tldr: 针对弦理论中卡拉比-丘流形度量计算困难的问题，本研究提出利用符号回归将复杂的神经网络近似模型蒸馏为简洁、可解释的解析公式。通过五项表达式实现了与神经网络相当的精度（R²=0.9994），且参数量减少了3000倍。该方法不仅揭示了几何约束下的核心特征，还能准确复现体积积分和汤川耦合等物理观测值，为复杂流形的解析建模提供了新途径。
motivation: 旨在解决卡拉比-丘流形度量缺乏解析表达式且计算成本高昂的问题，通过将黑盒神经网络转化为可解释的数学模型来提升研究深度。
method: 利用符号回归技术对神经网络训练的度量近似值进行蒸馏，提取出基于幂和及对称多项式的简洁函数形式。
result: 五项解析表达式在参数量大幅减少的情况下达到了极高的拟合精度，并成功复现了体积积分和汤川耦合等关键物理量。
conclusion: 符号蒸馏证明了可以从深度学习模型中提取出紧凑、可解释且具有物理有效性的解析模型，为几何物理研究开辟了新方向。
---

## 摘要
卡拉比-丘流形对弦理论至关重要，但其度规的计算往往难以处理。在本文中，我们展示了符号回归可以将神经网络近似蒸馏为简单、可解释的公式。我们的五项表达式在参数量减少 3000 倍的情况下，达到了与神经网络相当的精度（$R^2 = 0.9994$）。多种子验证确认了几何约束选择了关键特征，特别是幂和与对称多项式，同时允许结构多样性。在所研究的模空间范围（$ψ \in [0, 0.8]$）内，该函数形式可以保持不变，且系数平滑变化；我们将这些趋势解释为在局部训练的教师模型精度范围内（当 $ψ \neq 0$ 时，$σ \approx 8-9\%$）的经验假设。该公式重现了物理可观测物理量——体积积分和汤川耦合——验证了符号蒸馏能够为以前只能通过黑盒网络获取的物理量恢复出紧凑、可解释的模型。

## Abstract
Calabi--Yau manifolds are essential for string theory but require computing intractable metrics. Here we show that symbolic regression can distill neural approximations into simple, interpretable formulas. Our five-term expression matches neural accuracy ($R^2 = 0.9994$) with 3,000-fold fewer parameters. Multi-seed validation confirms that geometric constraints select essential features, specifically power sums and symmetric polynomials, while permitting structural diversity. The functional form can be maintained across the studied moduli range ($ψ\in [0, 0.8]$) with coefficients varying smoothly; we interpret these trends as empirical hypotheses within the accuracy regime of the locally-trained teachers ($σ\approx 8-9\%$ at $ψ\neq 0$). The formula reproduces physical observables -- volume integrals and Yukawa couplings -- validating that symbolic distillation recovers compact, interpretable models for quantities previously accessible only to black-box networks.

---

## 论文详细总结（自动生成）

这篇论文《Interpretable Analytic Calabi-Yau Metrics via Symbolic Distillation》探讨了如何利用机器学习技术解决弦理论中长期存在的数学难题。以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：在弦理论中，卡拉比-丘（Calabi-Yau, CY）流形的里奇平坦度规（Ricci-flat metrics）对于计算粒子物理观测值（如汤川耦合）至关重要。然而，描述这些度规的 Monge-Ampère 方程在数学上极难精确求解，传统数值方法计算成本极高。
*   **核心问题**：虽然近年来的神经网络（NN）代理模型能够快速近似度规，但它们是“黑盒”，缺乏物理直觉和解析表达。论文旨在探索：**能否从复杂的神经网络中“蒸馏”出既紧凑又具有解析可解释性的数学公式，且不损失预测精度？**

### 2. 方法论：符号蒸馏（Symbolic Distillation）
*   **核心思想**：采用“教师-学生”架构。首先训练一个高精度的神经网络（教师模型）作为度规的数值近似，然后利用符号回归（学生模型）从教师模型生成的数据中提取解析公式。
*   **关键技术细节**：
    *   **特征工程**：利用规范不变的几何不变量作为输入特征，包括幂和（Power Sums, $p_2$）和第三初等对称多项式（$\sigma_3$）。
    *   **符号回归**：使用 `PySR` 库，通过进化算法在数学算子空间中搜索最优函数形式。
    *   **目标函数**：回归目标是度规行列式与 Fubini-Study 度规行列式比值的对数：$y = \log(\det g_{alg} / \det g_{FS})$。
    *   **解析形式**：最终发现了一个仅含 5 个项的简洁公式，形式如下：
        $$\log \frac{\det g_{alg}}{\det g_{FS}} = c_0 + c_1 \frac{1}{p_2^2} + c_2 \frac{\sigma_3}{p_2^{3/2}} + c_3 p_2 + c_4 \sigma_3$$
    *   **模空间分解原理**：提出“分解假设”，即函数结构在模空间内保持稳定，而几何变形仅由平滑变化的系数 $c_i(\psi)$ 编码。

### 3. 实验设计
*   **数据集/场景**：研究了 Dwork 家族的卡拉比-丘三维流形，复结构模参数 $\psi$ 范围设定在 $[0, 0.8]$（从费马点到接近奇点）。
*   **Benchmark（基准）**：
    *   **教师模型**：基于 Donaldson 算法的 H-矩阵（k=10，含 875 个参数）。
    *   **对比方法**：Ashmore 等人的神经网络代理模型（约 15,000 个参数）、三阶多项式拟合。
*   **物理验证**：通过计算体积积分（Volume Integrals）和汤川耦合（Yukawa Couplings）来验证公式的物理有效性。

### 4. 资源与算力
*   **硬件使用**：论文明确提到符号回归搜索是在 **NVIDIA A100 GPU**（Google Colab Pro 环境）上运行的。
*   **训练时长**：符号回归运行了 200 次迭代；神经网络教师模型的训练在每个 $\psi$ 点上进行 15 次 Adam 优化迭代。
*   **效率提升**：符号公式在推理速度上比 Donaldson H-矩阵快 **10,000 倍**，比神经网络快 **3,000 倍**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   使用了 $10^5$ 个采样点进行训练和验证。
    *   **多种子验证**：进行了 10 组独立随机种子的符号回归实验，以确认特征选择（$p_2, \sigma_3$）的稳健性。
    *   **消融实验**：通过移除 $\sigma_3$ 特征验证了其在捕捉各向异性方面的必要性（误差降低了 50%）。
    *   **跨阶验证**：在不同的多项式阶数（k=6, 8, 10）下验证了系数的稳定性。
*   **评价**：实验设计较为充分，涵盖了从统计拟合优度（$R^2$）到深层物理量验证的多个维度，客观地展示了符号公式的泛化能力。

### 6. 主要结论与发现
*   **极高压缩比**：5 参数的符号公式达到了与 15,000 参数神经网络相当的精度（$R^2 = 0.9994$），参数量减少了 **3,000 倍**。
*   **分层调制原理**：在模空间移动时，公式的函数形式保持不变，但系数表现出分层响应：奇异项发生符号反转，而对称项随 $\psi$ 单调增强。
*   **物理一致性**：符号公式计算的汤川耦合与文献精确值完全匹配，体积积分误差在 5.5% 以内（受限于教师模型本身的精度）。

### 7. 优点
*   **可解释性**：将“黑盒”度规转化为人类可读的公式，揭示了幂和与对称多项式在里奇平坦性中的核心作用。
*   **科学发现的新范式**：展示了 AI 如何辅助理论物理学家发现潜在的数学结构（如分解假设）。
*   **实用性**：极大地降低了弦理论唯象学研究中扫描模空间的计算成本。

### 8. 不足与局限
*   **教师模型依赖**：符号公式的精度上限受限于教师模型（神经网络）的精度。在 $\psi \neq 0$ 时，教师模型的里奇平坦误差（$\sigma \approx 8-9\%$）限制了对更精细几何结构的探索。
*   **覆盖范围有限**：目前仅验证了 Dwork 家族（五次超曲面），尚未证明该特定函数形式是否能推广到其他类型的卡拉比-丘流形。
*   **经验性结论**：系数的符号反转等趋势目前属于经验观察，缺乏严格的解析数学证明。
*   **奇点限制**：研究范围避开了 $\psi \to 1$ 的锥形奇点（Conifold limit），在极端几何区域的适用性未知。

（完）
