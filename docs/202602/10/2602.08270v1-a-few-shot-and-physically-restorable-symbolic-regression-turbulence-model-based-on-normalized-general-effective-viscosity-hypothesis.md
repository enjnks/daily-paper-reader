---
title: A few-shot and physically restorable symbolic regression turbulence model based on normalized general effective-viscosity hypothesis
authors: "Ziqi Ji, Penghao Duan, Gang Du"
date: 2026-02-09
pdf: "https://arxiv.org/pdf/2602.08270v1"
tags: ["keyword:SR", "query:SR"]
score: 10.0
evidence: 用于物理湍流建模的新型符号回归模型
tldr: 针对RANS湍流模型精度不足及数据驱动模型泛化性差的问题，本文提出一种基于归一化通用有效粘性假设的少样本、物理可恢复符号回归湍流模型。该模型仅利用少量三维周期山流动数据训练，即可在多种复杂流动场景（如机翼、压气机）中超越基准模型，并能在特定物理条件下回归至基准模型，兼具高精度与物理一致性。
motivation: 旨在解决传统RANS模型精度受限以及现有数据驱动湍流模型在泛化能力和物理可解释性方面的不足。
method: 基于归一化通用有效粘性假设，利用少量DNS数据通过符号回归方法构建具有物理恢复特性的湍流模型。
result: 该模型在周期山、NACA0012机翼及NASA Rotor 37压气机等多种流动测试中均表现出优于基准模型的性能。
conclusion: 研究证明了符号回归在构建少样本、高泛化性且符合物理约束的湍流模型方面的巨大潜力。
---

## Abstract
Turbulence is a complex, irregular flow phenomenon ubiquitous in natural processes and engineering applications. The Reynolds-averaged Navier-Stokes (RANS) method, owing to its low computational cost, has become the primary approach for rapid simulation of engineering turbulence problems. However, the inaccuracy of classical turbulence models constitutes the main drawback of the RANS framework. With the rapid development of data-driven approaches, many data-driven turbulence models have been proposed, yet they still suffer from issues of generalizability and accuracy. In this work, we propose a few-shot, physically restorable, symbolic regression turbulence model based on the normalized general effective-viscosity hypothesis. Few-shot indicates that our model is trained on limited flow configurations spanning only a narrow subset of turbulent flow physics, yet can still outperform the baseline model in substantially different turbulent flows. Physically restorable means our model can nearly revert to the baseline model in regimes satisfying specific physical conditions, using only the symbolic regression training results. The normalized general effective-viscosity hypothesis was proposed in our previous study. Specifically, we first formalize the concept of few-shot data-driven turbulence models. Second, we train our symbolic regression turbulence models using only direct numerical simulation (DNS) data for three-dimensional periodic hill flow slices. Third, we evaluate our models on periodic hill flows, zero pressure gradient flat plate flow, NACA0012 airfoil flows, and NASA Rotor 37 transonic axial compressor flows. One of our symbolic regression turbulence models consistently outperforms the baseline model, and we further demonstrate that this model can nearly revert to baseline behavior in certain flow regimes.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种结合物理启发与数据驱动技术的符号回归（Symbolic Regression, SR）湍流模型。以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的雷诺平均纳维-斯托克斯（RANS）模型计算成本低但精度不足，而现有的数据驱动模型（如神经网络）往往面临泛化性差、缺乏物理可解释性以及需要海量高保真数据（DNS/LES）等挑战。
*   **研究动机**：
    *   **少样本学习（Few-shot）**：探索能否仅利用极少量的流动配置（如简单的周期山流动）训练模型，使其在完全不同的复杂工程流动中依然有效。
    *   **物理可恢复性（Physically Restorable）**：要求模型在满足特定物理条件（如近壁面或各向同性区域）时，能够自动退化回经典的基准模型（如 $k-\omega$-SST），以保留经典模型在简单流动中的校准优势。

### 2. 核心方法论
*   **归一化通用有效粘性假设**：基于 Pope (1975) 的理论，作者提出了归一化的张量基展开形式。通过对张量基进行归一化处理，使得各项系数（$\hat{g}_i$）具有可比性，便于符号回归算法捕捉主要物理贡献。
*   **关键技术细节**：
    *   **输入特征**：选取了 7 个张量不变量（涉及应变率、旋转率、压力梯度和湍能梯度）以及 5 个额外的物理特征（如壁面距离雷诺数、湍流时间尺度比等）。
    *   **符号回归工具**：使用开源库 **PySR**，通过多群体进化算法自动搜索显式的数学表达式。
    *   **模型构建**：对比了 **SR 3T**（包含 3 个二维流动独立张量）和 **SR 5T**（包含 5 个张量，涵盖三维效应）。
    *   **物理约束**：固定 $\hat{g}_1$ 项以匹配基准模型的线性涡粘性部分，仅对非线性项进行符号回归建模，确保模型在特定条件下（系数趋于零时）能恢复至 Boussinesq 假设。

### 3. 实验设计
*   **训练集**：仅使用参数化周期山（Periodic Hill）流动的 DNS 数据切片（$\alpha=0.8$ 和 $1.2$ 两种几何形状）。
*   **测试场景（跨度极大）**：
    1.  **周期山流动**（内插与外推几何形状）：验证基础预测能力。
    2.  **零压力梯度平板流动**：验证模型是否破坏了经典边界层理论的预测。
    3.  **NACA0012 机翼流动**：验证高雷诺数下的分离流预测。
    4.  **NASA Rotor 37 跨声速轴流压气机**：验证在复杂三维、可压缩、强激波工程流动中的泛化性。
*   **Benchmark（基准）**：经典的 $k-\omega$-SST 模型，以及部分案例中对比了基于神经网络的湍流模型（SA-NN）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号或训练总时长。
*   **算法参数**：提到了 PySR 的超参数设置，包括 16 个种群、每个种群 100 个个体、迭代 2000 次、方程最大复杂度为 20。
*   **软件环境**：使用 TCAE（基于 OpenFOAM）进行后验 CFD 仿真。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从低速到跨声速、从二维到三维、从简单几何到复杂旋转机械的 4 大类测试案例。
*   **充分性评价**：实验设计非常充分且具有挑战性。特别是从简单的周期山流动（训练集）直接跨越到 NASA Rotor 37（测试集），这种“少样本”到“强泛化”的验证极具说服力。
*   **客观性**：通过 MSE（均方误差）定量分析和流场云图定性分析相结合，对比了 SR 3T、SR 5T 与基准模型，结果表现出较好的客观性。

### 6. 主要结论与发现
*   **SR 5T 表现最优**：在所有测试案例中，SR 5T 模型均优于基准 $k-\omega$-SST 模型，尤其在预测流动分离和压气机效率方面提升显著。
*   **泛化能力强**：模型证明了即使在不可压缩流动上训练，也能捕捉到跨声速激波相关的湍流特性。
*   **物理恢复性验证**：在平板流动的粘性底层和压气机下游的各向同性区域，SR 5T 的非线性项系数自动趋于零，成功实现了向基准模型的物理回归。
*   **张量选择的重要性**：SR 3T 在某些情况下表现甚至不如基准模型，说明在处理复杂流动时，引入额外的非线性张量基（如 SR 5T）至关重要。

### 7. 优点与亮点
*   **显式表达式**：符号回归生成的模型是显式的数学公式，易于植入任何 CFD 软件，且无需神经网络推理所需的额外计算开销。
*   **少样本高效性**：仅需极少量 DNS 数据即可训练出高泛化性的模型，极大降低了对昂贵数据的依赖。
*   **物理一致性**：通过归一化框架和特征选择，使模型在数学形式上满足物理约束，避免了纯黑盒模型的不可控性。

### 8. 不足与局限
*   **公式选择的经验性**：虽然符号回归是自动的，但在多个候选方程中选择最终模型时，仍依赖于研究者的专家经验（平衡复杂性与准确性）。
*   **局部过拟合风险**：在 NASA Rotor 37 的上游区域，模型出现了一些非预期的激活（可能是由于周期山训练数据的局限性导致的局部过拟合）。
*   **计算稳定性**：虽然论文提到模型在跨声速计算中表现稳定，但符号回归产生的非线性项在某些极端工况下是否会导致数值发散仍需更广泛的验证。

（完）
