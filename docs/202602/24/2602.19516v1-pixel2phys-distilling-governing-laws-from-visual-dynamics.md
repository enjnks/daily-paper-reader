---
title: "Pixel2Phys: Distilling Governing Laws from Visual Dynamics"
title_zh: Pixel2Phys：从视觉动力学中提炼控制方程
authors: "Ruikun Li, Jun Yao, Yingfan Hua, Shixiang Tang, Biqing Qi, Bin Liu, Wanli Ouyang, Yan Lu"
date: 2026-02-23
pdf: "https://arxiv.org/pdf/2602.19516v1"
tags: ["keyword:SR", "query:SR"]
score: 8.0
evidence: 从视觉数据中提取形式化的物理知识和支配规律
tldr: 本研究提出Pixel2Phys，一个旨在从高维视觉数据中直接提取物理规律的多智能体框架。针对视频数据冗余度高、物理信息难以提取的挑战，该框架利用多模态大模型（MLLM）模拟人类科学推理过程，通过迭代生成、验证和优化假设，从原始观测中自动发现简洁且可解释的控制方程。实验证明，该方法在模拟和真实场景中均能准确识别物理规律，并具备优异的长期外推预测能力。
motivation: 从高维且冗余的原始视频观测中提取低维、结构化的物理规律是科学智能领域的一大挑战。
method: 提出一种适配多模态大模型的多智能体协作框架，通过迭代的假设生成、验证与精炼流程来提取物理知识。
result: 在多种模拟和真实物理视频中成功发现了准确、可解释的控制方程，并实现了稳定的长期外推。
conclusion: Pixel2Phys 模拟了人类科学家的探索过程，证明了人工智能具备直接从视觉观测中揭示底层物理规律的能力。
---

## 摘要
直接从高维视觉数据中发现物理定律是人类长期的追求，但对机器而言仍是一个巨大的挑战，这代表了科学智能的一个基本目标。这一任务本质上非常困难，因为物理知识是低维且结构化的，而原始视频观测数据是高维且冗余的，其中大多数像素几乎不携带物理意义。从此类噪声数据中提取简洁且具有物理相关性的变量仍然是一个主要障碍。为了解决这一问题，我们提出了 Pixel2Phys，这是一个可适配于任何多模态大语言模型（MLLM）的协作式多智能体框架。它通过采用结构化的工作流，利用迭代的假设生成、验证和优化来提取形式化的物理知识，从而模拟人类的科学推理。通过在高维数据上反复构建和优化候选方程，它能够识别出最能捕捉底层物理演化的简洁表示。这种自动化探索模拟了人类科学家的迭代工作流程，使人工智能能够直接从原始观测中揭示可解释的控制方程。在多种模拟和真实世界的物理视频中，Pixel2Phys 发现了准确且可解释的控制方程，并在基准模型迅速发散的情况下保持了稳定的长期外推。

## Abstract
Discovering physical laws directly from high-dimensional visual data is a long-standing human pursuit but remains a formidable challenge for machines, representing a fundamental goal of scientific intelligence. This task is inherently difficult because physical knowledge is low-dimensional and structured, whereas raw video observations are high-dimensional and redundant, with most pixels carrying little or no physical meaning. Extracting concise, physically relevant variables from such noisy data remains a key obstacle. To address this, we propose Pixel2Phys, a collaborative multi-agent framework adaptable to any Multimodal Large Language Model (MLLM). It emulates human scientific reasoning by employing a structured workflow to extract formalized physical knowledge through iterative hypothesis generation, validation, and refinement. By repeatedly formulating, and refining candidate equations on high-dimensional data, it identifies the most concise representations that best capture the underlying physical evolution. This automated exploration mimics the iterative workflow of human scientists, enabling AI to reveal interpretable governing equations directly from raw observations. Across diverse simulated and real-world physics videos, Pixel2Phys discovers accurate, interpretable governing equations and maintaining stable long-term extrapolation where baselines rapidly diverge.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **Pixel2Phys** 的多智能体协作框架，旨在直接从高维视频数据中自动发现底层的物理控制方程。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心挑战**：物理规律通常是低维且结构化的（如 $F=ma$），而原始视频观测是高维且冗余的（充满噪声、光影、背景）。
*   **现有局限**：
    1.  **监督模型**：依赖稀缺的“视频-方程”对，泛化性差。
    2.  **无监督潜空间方法**：学习到的变量往往缺乏物理含义，且存在“循环依赖”问题（提取好变量需要知道动力学，发现动力学需要干净的变量）。
    3.  **直接使用大模型**：MLLM 往往只是检索训练语料中的知识，难以从原始像素中推导新规律。
*   **研究目标**：模拟人类科学家的推理过程，通过迭代的“观察-假设-实验-修正”流程，从视觉动力学中提炼出简洁、可解释的控制方程。

### 2. 方法论：多智能体协作框架
Pixel2Phys 将物理发现任务分解为四个专门的智能体，由多模态大语言模型（如 GPT-4o）驱动：
*   **核心思想**：通过**协同优化（Co-optimization）**打破变量提取与规律发现之间的循环依赖。
*   **关键组件**：
    1.  **规划智能体 (Plan Agent)**：作为协调者，分析实验报告，诊断瓶颈（是变量提取不准还是方程太复杂），并下达改进指令。
    2.  **变量智能体 (Variable Agent)**：提供多粒度工具箱。
        *   **物体级**：利用 SAM 等模型提取物体轨迹。
        *   **像素级**：利用卷积核计算空间导数（如拉普拉斯算子）。
        *   **表示级**：使用**物理信息自动编码器 (Physics-informed AE)**，其损失函数包含重构误差和物理一致性损失 $L = L_{recon} + \lambda_{eq} \| \dot{z} - f(z) \|^2$，强制潜空间符合当前发现的方程。
    3.  **方程智能体 (Equation Agent)**：执行动态符号回归。利用 SINDy 算法在候选函数库（多项式、三角函数等）中通过稀疏回归（STLSQ）寻找最简方程。
    4.  **实验智能体 (Experiment Agent)**：验证 (Z, f) 对。通过数值模拟预测未来轨迹，计算 $R^2$、RMSE 和复杂度，生成可视化报告反馈给规划者。

### 3. 实验设计
论文在三类不同难度的场景中进行了验证：
*   **物体运动 (Object-level)**：包括线性、三次方、圆周运动、Van Der Pol 振子和滑翔机系统。
    *   **Benchmark**: AE-SINDy, Latent-ODE, Coord-Equ。
*   **物理场 (Pixel-level)**：四种反应扩散方程（LO, Bruss, FHN, SH）。
    *   **Benchmark**: 神经算子（FNO, UNO, WNO）和符号回归方法（PDE-Find, SGA-PDE, LLM-PDE）。
*   **真实科学现象 (Representation-level)**：卡门涡街（流体）和 BZ 化学振荡反应。
    *   **Benchmark**: FNO, Latent-ODE, 以及先进的视频生成模型 Wan2.2。

### 4. 资源与算力
*   **模型后端**：默认使用 **GPT-4o** 作为 LLM 后端。
*   **算力说明**：文中**未明确给出**具体的 GPU 训练时长或硬件数量。由于该框架主要基于 Agent 调度和现有的视觉基础模型（如 SAM），其核心开销在于 LLM 的 API 调用和轻量级的符号回归计算，而非大规模端到端训练。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从简单质点运动到复杂偏微分方程（PDE），再到真实世界噪声视频的跨度。
*   **消融实验**：验证了“规划智能体”和“迭代反馈循环”的必要性。
*   **鲁棒性测试**：测试了在不同 LLM 后端（如较小规模模型）下的表现。
*   **评价指标**：使用了 $R^2$ 分数、RMSE、有效预测步数 (VPS) 和方程项匹配度，评估维度较为全面、客观。

### 6. 主要结论与发现
*   **性能提升**：Pixel2Phys 在长期外推预测的准确度（RMSE）上比基准模型提升了 **45.35%**。
*   **稳定性**：相比于黑盒神经算子（容易随时间发散），Pixel2Phys 发现的符号方程在长期模拟中表现出极高的稳定性。
*   **物理保真度**：即使在真实世界的噪声视频中，该框架也能过滤掉光影等无关因素，提取出反映本质动力学的低维吸引子。
*   **超越生成模型**：相比于 Wan2.2 等视频生成模型，Pixel2Phys 能够保持物理一致性（如严格的振荡周期），而生成模型常出现空间漂移。

### 7. 优点与亮点
*   **迭代闭环**：通过“物理信息反馈”解决了视觉特征提取中的噪声干扰问题，模拟了真实的科学探索逻辑。
*   **多粒度适配**：一个框架同时处理质点、场和隐变量空间，通用性强。
*   **可解释性**：输出的是显式的数学公式，而非黑盒权重，直接服务于科学发现。

### 8. 不足与局限
*   **对 LLM 的依赖**：框架的推理能力高度依赖于底层大模型的逻辑水平，在处理极度复杂的非线性项时可能需要更多迭代。
*   **函数库限制**：符号回归的效果受限于预设的候选算子库，对于包含极其罕见数学形式的物理规律可能难以直接发现。
*   **实时性**：由于涉及多轮 Agent 交互和数值模拟，该流程目前更适合离线科学分析，而非实时视觉任务。

（完）
