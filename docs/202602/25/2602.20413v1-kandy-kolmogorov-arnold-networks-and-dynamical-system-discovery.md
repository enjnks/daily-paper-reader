---
title: "KANDy: Kolmogorov-Arnold Networks and Dynamical System Discovery"
title_zh: KANDy：柯尔莫哥洛夫-阿诺德网络与动力系统发现
authors: "Kevin Slote, Jeremie Fish, Erik Bollt"
date: 2026-02-23
pdf: "https://arxiv.org/pdf/2602.20413v1"
tags: ["keyword:SR", "query:SR"]
score: 10.0
evidence: 直接提出了一种基于KAN的架构，用于发现动力系统中的控制方程。
tldr: 本研究提出KANDy，一种基于Kolmogorov-Arnold网络（KAN）的零深度、宽架构神经网络，旨在发现混沌及复杂动力系统的控制方程。KANDy通过将KAN与稀疏回归相结合，克服了传统KAN在动力系统应用中的局限性，并解决了稀疏回归受限于稀疏性约束的问题。该模型在离散、连续系统及混沌偏微分方程中表现出色，能有效恢复拓扑结构，为非线性动力系统的数据驱动建模提供了一种可解释且高效的新方案。
motivation: 旨在解决传统稀疏回归在复杂动力系统建模中的局限性，并提升KAN在方程发现任务中的可解释性。
method: 提出一种零深度的宽架构网络KANDy，通过将KAN与稀疏回归思想融合来显式学习系统的控制方程。
result: 在离散系统、连续系统及混沌偏微分方程上成功发现了控制方程，并能准确恢复Hopf纤维化等拓扑结构。
conclusion: KANDy作为一种可解释的建模工具，为复杂非线性动力系统的方程发现和动力学分析提供了有效替代方案。
---

## 摘要
我们引入了动力学柯尔莫哥洛夫-阿诺德网络（KANDy），这是一种零深度、宽神经架构，能够发现混沌且复杂的动力系统中的控制方程。在柯尔莫哥洛夫-阿诺德网络（KANs）的基础上，KANDy 通过用 KAN 替代稀疏回归，显式地学习控制方程。KANs 与稀疏回归的结合解决了 KANs 应用于动力系统时在方程发现方面的局限性，并克服了稀疏回归受限于稀疏性约束的情况。此外，我们展示了该模型在应用于 Hopf 纤维化时能够恢复拓扑结构，从而提高了与吸引子特性的一致性。我们将该模型应用于离散和连续动力系统，以及混沌偏微分方程（PDEs）。这些结果使 KANDy 成为非线性动力系统数据驱动建模中一种可解释且有效的替代方案。

## Abstract
We introduce the Kolmogorov-Arnold Network for Dynamics (KANDy) as a zero-depth, wide neural architecture capable of discovering governing equations in chaotic and complex dynamical systems. Building on the foundation of Kolmogorov-Arnold Networks (KANs), KANDy explicitly learns governing equations by replacing sparse regression with a KAN. The synthesis of KANs and sparse regression addresses the limitations of equation discovery for KANs applied to dynamical systems and overcomes cases where sparse regression is hindered by sparsity constraints. Additionally, we show that our model, applied to the Hopf Fibration, recovers topological structure, thereby improving coherence with attractor properties. We apply our model to discrete and continuous dynamical systems, as well as to chaotic partial differential equations (PDEs). These results position KANDy as an interpretable and effective alternative for data-driven modeling of nonlinear dynamical systems.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **KANDy**（Kolmogorov-Arnold Network for Dynamics）的新型架构，旨在结合柯尔莫哥洛夫-阿诺德网络（KAN）的灵活性与稀疏回归的可解释性，用于发现复杂动力系统的控制方程。

以下是对该论文的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在数据驱动的建模中，既能处理混沌系统的高度非线性，又能提取出简洁、可解释的数学方程？
*   **研究动机**：
    *   **稀疏回归（如 SINDy）的局限性**：这类方法假设方程由少数基函数组成。但在生物或光学系统中（如 Ikeda 映射、Holling 型项），方程可能包含无限级数或非稀疏项，导致稀疏回归失效。
    *   **深度学习与 KAN 的不足**：传统的深度神经网络（MLP）是黑箱；而原始的 KAN 虽然具有可解释性，但在处理多变量耦合项（如 Lorenz 系统中的 $xy$）时，由于其单变量样条函数的限制，难以直接提取全局控制方程。
    *   **混沌预测难题**：混沌系统对初始条件极度敏感，预测视野受限于李雅普诺夫时间（Lyapunov time）。

### 2. 论文提出的方法论：KANDy 架构
*   **核心思想**：采用**零深度（Zero-depth）、宽架构（Wide）**的 KAN。它不依赖增加网络深度，而是通过扩展输入层的“提升特征”（Lifted Features）来捕捉非线性。
*   **关键技术细节**：
    *   **坐标提升（Lifting）**：将原始状态变量 $x$ 通过提升函数 $\Phi(x)$ 映射到更高维的空间（包含物理启发项，如 $xy, x^2, \sin(x)$ 等）。
    *   **零深度 KAN**：在提升后的特征上直接应用单层 KAN。这相当于一种广义的非线性回归，但其激活函数是可学习的样条函数。
    *   **混合损失函数**：结合了**导数监督（Derivative Loss）**和**轨迹滚动监督（Rollout Loss）**。公式表示为：$L = L_{deriv} + \lambda_{roll} L_{roll}$。这种设计确保了模型不仅在局部向量场上准确，在长期轨迹演化上也具有全局一致性。
    *   **符号公式提取**：实现了一种基于 $R^2$ 拟合优度和符号复杂性惩罚的贪心边缘选择算法，将学习到的样条函数转化为闭合形式的数学表达式。

### 3. 实验设计
*   **实验场景与数据集**：
    *   **离散动力系统**：Hénon 映射、Ikeda 光学腔模型（用于测试非稀疏项发现）。
    *   **连续动力系统**：Lorenz 系统（经典的低维混沌基准）。
    *   **偏微分方程（PDE）**：Kuramoto-Sivashinsky (KS) 方程（时空混沌）、无粘性 Burgers 方程（冲击波形成）。
    *   **拓扑结构测试**：Hopf 纤维化（Hopf Fibration），用于验证模型恢复流形拓扑和对称性的能力。
*   **Benchmark 与对比方法**：
    *   对比了**标准稀疏回归**（在非稀疏项上表现不佳）。
    *   对比了**深度 KAN**（证明了深度在方程发现任务中反而可能导致结构模糊）。

### 4. 资源与算力
*   论文中**未明确说明**具体的 GPU 型号或总算力消耗。
*   **训练细节提到**：Lorenz 系统训练了 300 个 epoch，学习率为 $10^{-3}$，使用了 4 阶龙格-库塔（RK4）积分器。
*   由于 KANDy 采用零深度架构，其参数量远小于深度模型，计算效率理论上高于深层 KAN 或大型神经 ODE。

### 5. 实验数量与充分性
*   **实验覆盖面**：涵盖了从 2D 映射到 3D ODE 再到高维 PDE 的多种复杂度等级，实验设计较为全面。
*   **充分性评价**：
    *   **多样性高**：不仅做了方程发现，还做了长期轨迹预测（Rollout）和吸引子几何恢复的评估。
    *   **客观性**：通过李雅普诺夫时间（Lyapunov time）来量化预测误差的增长，符合动力系统研究的标准。
    *   **消融/对比**：通过 Hopf 纤维化实验对比了深度 KAN 与 KANDy，有力证明了“提升特征”比“增加深度”在处理特定非线性时更有效。

### 6. 论文的主要结论与发现
*   **深度并非必要**：在方程发现任务中，零深度、宽架构的 KAN 比深层 KAN 更容易提取出准确的符号方程。
*   **克服稀疏性限制**：KANDy 成功发现了 Ikeda 映射等稀疏回归难以处理的系统方程。
*   **保持吸引子几何**：模型在超过李雅普诺夫时间后，虽然相位会漂移，但仍能准确维持吸引子的拓扑形状。
*   **拓扑恢复**：KANDy 能够从数据中学习到复杂的纤维丛结构（如 Hopf 纤维化），这是传统回归方法难以企及的。

### 7. 优点（亮点）
*   **可解释性极强**：直接输出闭合形式的数学方程，而非黑箱模型。
*   **鲁棒性**：通过引入 Rollout Loss，解决了纯导数拟合在长期预测中容易发散的问题。
*   **物理启发**：将 Koopman 算子理论中的“提升”思想与 KAN 结合，具有坚实的数学基础。

### 8. 不足与局限
*   **库依赖性**：虽然比稀疏回归更灵活，但仍依赖于预定义的提升特征库（Library）。如果真实的物理项不在库中，发现难度会增加。
*   **外推风险**：实验主要集中在吸引子内部，对于远离训练分布的初始条件，模型可能会发散。
*   **高维扩展性**：对于极高维的 PDE，提升特征的数量可能会呈组合爆炸式增长，如何高效筛选特征仍是挑战。

（完）
