---
title: "GENSR: Symbolic Regression Based in Equation Generative Space"
title_zh: GenSR：基于方程生成空间的符号回归
authors: "Qian Li, Yuxiao Hu, Juncheng Liu, Yuntian Chen"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.20557v1"
tags: ["keyword:SR", "query:SR"]
score: 10.0
evidence: 直接提出了一种基于生成潜空间的创新符号回归框架
tldr: 针对符号回归在离散空间搜索时结构变化与数值行为不一致的挑战，本文提出GenSR框架。该框架通过预训练双分支条件变分自编码器（CVAE），将离散方程映射到具有符号连续性和数值平滑性的生成潜空间。通过“地图构建、粗略定位、精细搜索”的范式，结合CMA-ES优化算法，GenSR实现了高效的方程发现，在精度、简洁性和鲁棒性上均表现出色。
motivation: 传统符号回归在离散空间中搜索，方程结构的微小改动往往导致数值行为剧烈波动，使得拟合误差反馈难以有效指导搜索过程。
method: 利用CVAE构建连续的方程生成潜空间，并采用“定位+精细搜索”的策略，通过CMA-ES在潜空间中利用平滑梯度寻找最优方程。
result: 实验证明GenSR在保持高预测精度的同时，显著提升了表达式的简洁度和计算效率，并展现出强大的抗噪声能力。
conclusion: GenSR通过将符号回归重构为潜空间优化问题，为解决离散方程搜索难题提供了一种具备理论保障且高效的新范式。
---

## 摘要
符号回归 (SR) 旨在揭示观测数据背后隐藏的方程。然而，大多数方法在离散的方程空间中进行搜索，其中方程的结构修改很少与其数值行为保持一致，导致拟合误差反馈过于嘈杂，难以引导探索。为了应对这一挑战，我们提出了 GenSR，这是一个基于生成潜空间的符号回归框架，遵循“地图构建 -> 粗略定位 -> 精细搜索”的范式。具体而言，GenSR 首先预训练一个双分支条件变分自编码器 (CVAE)，将符号方程重新参数化为具有符号连续性和局部数值平滑性的生成潜空间。该空间可被视为方程空间中结构良好的“地图”，为搜索提供方向性信号。在推理阶段，CVAE 将输入数据粗略定位到潜空间中有潜力的区域。随后，改进的 CMA-ES 利用平滑的潜梯度对候选区域进行精细化搜索。从贝叶斯角度来看，GenSR 将符号回归任务重新定义为最大化条件分布 $p(\mathrm{Equ.} \mid \mathrm{Num.})$，而 CVAE 的训练通过证据下界 (ELBO) 实现了这一目标。这一新视角为 GenSR 的有效性提供了理论保证。大量实验表明，GenSR 在保持对噪声鲁棒性的同时，共同优化了预测准确性、表达式简洁性和计算效率。

## Abstract
Symbolic Regression (SR) tries to reveal the hidden equations behind observed data. However, most methods search within a discrete equation space, where the structural modifications of equations rarely align with their numerical behavior, leaving fitting error feedback too noisy to guide exploration. To address this challenge, we propose GenSR, a generative latent space-based SR framework following the `map construction -> coarse localization -> fine search'' paradigm. Specifically, GenSR first pretrains a dual-branch Conditional Variational Autoencoder (CVAE) to reparameterize symbolic equations into a generative latent space with symbolic continuity and local numerical smoothness. This space can be regarded as a well-structured `map'' of the equation space, providing directional signals for search. At inference, the CVAE coarsely localizes the input data to promising regions in the latent space. Then, a modified CMA-ES refines the candidate region, leveraging smooth latent gradients. From a Bayesian perspective, GenSR reframes the SR task as maximizing the conditional distribution $p(\mathrm{Equ.} \mid \mathrm{Num.})$, with CVAE training achieving this objective through the Evidence Lower Bound (ELBO). This new perspective provides a theoretical guarantee for the effectiveness of GenSR. Extensive experiments show that GenSR jointly optimizes predictive accuracy, expression simplicity, and computational efficiency, while remaining robust under noise.

---

## 论文详细总结（自动生成）

以下是对论文《GENSR: Symbolic Regression Based in Equation Generative Space》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：传统的符号回归（SR）主要在离散的方程空间中通过启发式算法（如遗传算法、MCTS）进行搜索。这种方式存在一个根本性矛盾：方程的**结构相似性**（编辑距离）与其**数值行为**（拟合误差）往往是不一致的。微小的结构改变可能导致数值结果剧烈波动，使得误差反馈充满噪声，难以引导搜索过程。
*   **核心目标**：构建一个连续且生成式的方程潜空间（Latent Space），将离散的符号搜索转化为在结构良好、数值平滑的“地图”中进行连续优化，从而提升搜索效率和方程的可解释性。

### 2. 方法论
GenSR 遵循 **“地图构建 -> 粗略定位 -> 精细搜索”** 的新范式：
*   **双分支 CVAE（核心架构）**：
    *   **后验分支（Posterior Branch）**：输入“方程+数值样本”，学习方程的结构特征，确保潜空间具备**符号连续性**（即潜空间中的点能解码为有效的方程）。
    *   **先验分支（Prior Branch）**：仅输入“数值样本”，学习数值行为到潜空间的映射，确保潜空间具备**局部数值平滑性**。
    *   **对齐机制**：通过最小化两个分支分布之间的 KL 散度，将数值特征与符号结构耦合，使得数值行为相似的方程在潜空间中距离更近。
*   **贝叶斯视角**：论文证明了 GenSR 的训练本质上是在最大化证据下界（ELBO），将 SR 任务重构为条件概率 $p(\text{Equation} | \text{Numeric Data})$ 的推断问题。
*   **潜空间搜索算法**：
    *   **粗略定位**：利用预训练的先验分支将输入数据映射到潜空间的高概率区域。
    *   **精细搜索（改进型 CMA-ES）**：在潜空间中运行协方差矩阵自适应进化策略。为了应对高维挑战，提出了**对角协方差假设**和 **Top-k 维度更新策略**，仅优化最相关的潜维度，极大地提高了收敛速度。

### 3. 实验设计
*   **数据集**：采用了符号回归领域的权威基准 **SRBench**，包含：
    *   **Feynman 数据集**：119 个物理学方程。
    *   **Strogatz 数据集**：14 个非线性动力系统方程。
    *   **Black-box 数据集**：57 个真实世界的黑盒回归任务。
*   **对比方法（18 种）**：
    *   **启发式/遗传算法**：GPlearn, Operon, GP-GOMEA 等。
    *   **深度学习/强化学习**：DSR, E2ESR, SNIP, TPSR, RAG-SR 等。
    *   **物理启发式**：AI Feynman 2.0。
    *   **传统机器学习**：XGBoost, Random Forest（作为基准参考）。
*   **评估指标**：拟合优度（$R^2$）、方程复杂度（Token 数量）、计算时间（秒）。

### 4. 资源与算力
*   **硬件设施**：使用了 **2 个 NVIDIA H800 GPU** (80GB 显存)。
*   **训练细节**：
    *   预训练数据集包含约 **500 万** 个合成方程-样本对。
    *   训练时长：共 200 个 epoch，每个 epoch 约耗时 1 小时（总计约 **400 GPU 小时**）。
    *   模型参数量：约 **1.62 亿** (162.1M)。

### 5. 实验数量与充分性
*   **实验规模**：非常充分。除了在三大标准基准上进行大规模测试外，还进行了：
    *   **抗噪性实验**：在 0 到 0.1 的四种噪声水平下测试稳定性。
    *   **消融实验**：验证了潜空间维度、CMA-ES 参数（k 值、种群大小、步长）对结果的影响。
    *   **潜空间分析**：通过 t-SNE 可视化、线性插值和分类任务验证了潜空间的结构化特性。
*   **客观性**：使用了第三方维护的 SRBench 框架，对比了当前最先进的（SOTA）各类方法，实验结果具有较高的公信力。

### 6. 主要结论与发现
*   **性能领先**：GenSR 在精度、复杂度和效率的 Pareto 前沿上表现优异，尤其在 Feynman 和 Strogatz 数据集上，其 $R^2$ 排名和简洁度排名均处于第一梯队。
*   **鲁棒性强**：在噪声环境下，GenSR 的性能下降比基于离散搜索的方法更缓慢，证明了连续潜空间对数据扰动的容忍度更高。
*   **搜索效率**：相比于需要数小时运行的传统 GP 方法，GenSR 结合 CMA-ES 可以在数十秒到数分钟内找到高质量解。

### 7. 优点与亮点
*   **理论创新**：将符号回归从“组合优化问题”转化为“贝叶斯推断下的连续优化问题”，并给出了严谨的 ELBO 证明。
*   **空间特性**：成功构建了一个既能保证方程合法解码（生成性），又能反映数值相似性（平滑性）的潜空间。
*   **搜索范式**：三阶段搜索策略有效解决了符号回归中“大海捞针”的难题，通过先验分布缩小了搜索范围。

### 8. 不足与局限
*   **预训练依赖**：模型的性能高度依赖于预训练方程库的分布。如果目标方程包含预训练中未见过的特殊算子或极端结构，模型可能难以发现。
*   **算力门槛**：虽然推理很快，但预训练阶段需要高性能 GPU 集群和大量时间，普通开发者难以从头复现预训练过程。
*   **常数处理**：虽然引入了 BFGS 优化常数，但在潜空间搜索过程中，如何更完美地解耦“结构搜索”与“精确常数微调”仍有优化空间。

（完）
