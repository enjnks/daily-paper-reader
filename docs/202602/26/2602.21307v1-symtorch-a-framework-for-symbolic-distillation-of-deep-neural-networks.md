---
title: "SymTorch: A Framework for Symbolic Distillation of Deep Neural Networks"
title_zh: SymTorch：深度神经网络符号蒸馏框架
authors: "Elizabeth S. Z. Tan, Adil Soubki, Miles Cranmer"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.21307v1"
tags: ["keyword:SR", "query:SR"]
score: 10.0
evidence: 直接涉及符号回归和 PySR 集成
tldr: "SymTorch 是一个旨在简化深度神经网络符号蒸馏的框架。它通过集成 PySR，自动将复杂的神经组件替换为可解释的数学表达式。该框架解决了 GPU-CPU 数据传输、缓存和模型序列化等工程障碍，支持在 GNN、PINN 和 Transformer 等多种架构中无缝切换神经与符号前向传播。实验表明，它能有效发现物理规律，并在 LLM 推理中通过替换 MLP 层实现 8.3% 的吞吐量提升。"
motivation: 尽管符号蒸馏在发现物理规律方面具有潜力，但由于将符号回归集成到深度学习工作流中存在巨大的工程障碍，其应用受到限制。
method: SymTorch 通过包装神经网络组件、收集输入输出行为，并利用 PySR 将其近似为人类可读的方程，实现了自动化的蒸馏流程。
result: "该框架在多种架构上得到验证，并在大语言模型推理实验中通过符号替代 MLP 层，实现了 8.3% 的吞吐量提升。"
conclusion: SymTorch 降低了符号蒸馏的技术门槛，为构建更具可解释性和更高推理效率的深度学习模型提供了有力工具。
---

## 摘要
符号蒸馏（Symbolic distillation）将神经网络或其组件替换为可解释的、闭式数学表达式。这种方法在直接从训练好的深度学习模型中发现物理定律和数学关系方面展现出潜力，但由于将符号回归集成到深度学习工作流中存在工程障碍，其应用仍然受限。我们推出了 SymTorch，这是一个通过封装神经网络组件、收集其输入输出行为并利用 PySR 将其近似为人类可读方程来自动化此蒸馏过程的库。SymTorch 处理了阻碍应用的工程挑战：GPU-CPU 数据传输、输入输出缓存、模型序列化以及神经前向传递与符号前向传递之间的无缝切换。我们在包括 GNN、PINN 和 Transformer 模型在内的多种架构上演示了 SymTorch。最后，我们提出了一个通过用符号代理替换 MLP 层来加速大语言模型（LLM）推理的概念验证，在性能适度下降的情况下实现了 8.3% 的吞吐量提升。

## Abstract
Symbolic distillation replaces neural networks, or components thereof, with interpretable, closed-form mathematical expressions. This approach has shown promise in discovering physical laws and mathematical relationships directly from trained deep learning models, yet adoption remains limited due to the engineering barrier of integrating symbolic regression into deep learning workflows. We introduce SymTorch, a library that automates this distillation by wrapping neural network components, collecting their input-output behavior, and approximating them with human-readable equations via PySR. SymTorch handles the engineering challenges that have hindered adoption: GPU-CPU data transfer, input-output caching, model serialization, and seamless switching between neural and symbolic forward passes. We demonstrate SymTorch across diverse architectures including GNNs, PINNs and transformer models. Finally, we present a proof-of-concept for accelerating LLM inference by replacing MLP layers with symbolic surrogates, achieving an 8.3\% throughput improvement with moderate performance degradation.

---

## 论文详细总结（自动生成）

以下是对论文《SymTorch: A Framework for Symbolic Distillation of Deep Neural Networks》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
深度学习模型虽然性能强大，但其“黑盒”性质限制了在科学发现（如物理、天文）中的应用，因为科学家需要的是简洁、可解释的数学定律（如 $F=ma$）。**符号回归（Symbolic Regression, SR）** 能够将数据拟合为闭式方程，是实现模型可解释性的有力工具。然而，将 SR 集成到现有的深度学习工作流（如 PyTorch）中存在巨大的工程障碍，包括 GPU-CPU 数据传输、激活值缓存、模型序列化以及神经组件与符号组件的混合调用。

论文提出了 **SymTorch**，这是一个基于 PyTorch 的自动化框架，旨在降低符号蒸馏的门槛，让研究人员能够轻松地将神经网络组件（如 MLP、GNN 的边函数等）转化为人类可读的数学公式。

### 2. 方法论：核心思想与关键技术
SymTorch 的核心思想是通过包装（Wrapping）神经网络组件，在推理过程中收集输入输出数据，并利用 **PySR**（一种基于遗传算法的符号回归库）寻找最优的数学表达式。

*   **核心组件 `SymbolicModel`**：继承自 `nn.Module`，可以包裹任何 PyTorch 模块或函数。
*   **自动化流程**：
    1.  **数据收集**：利用 `forward hooks` 自动记录指定组件的输入和输出张量。
    2.  **符号蒸馏（Distill）**：调用 PySR 在收集的数据上进行多群体进化搜索，寻找在准确性和复杂度之间取得平衡的方程（Pareto 前沿）。
    3.  **无缝切换**：支持 `switch_to_symbolic` 方法，在不改变模型结构的情况下，在前向传播中直接用生成的数学方程替换原始神经运算。
*   **关键技术细节**：
    *   **SLIME 实现**：内置了符号化的 LIME（SLIME），通过在局部邻域采样并拟合符号方程来解释黑盒模型的局部行为。
    *   **降维集成**：针对高维输入（如 LLM 的 MLP 层），集成了 PCA 降维技术，使 SR 在处理高维特征时更具计算可行性。
    *   **变量转换**：允许用户定义特征转换（如坐标转距离 $r$），引导 SR 搜索更符合物理直觉的解空间。

### 3. 实验设计
论文设计了四个具有代表性的案例研究：
*   **LLM MLP 替代**：使用 **Qwen2.5-1.5B** 模型，在 Wikitext-2 数据集上将部分 MLP 层替换为符号方程，对比基准包括原始模型、PCA 压缩模型和恒等变换控制组。
*   **GNN 物理定律发现**：在粒子动力学数据集上训练图神经网络（GNN），蒸馏其边函数以恢复引力、弹力等物理定律。对比了 Standard、Bottleneck、L1、KL 和 Pruning（剪枝）等多种 GNN 变体。
*   **PINN 偏微分方程恢复**：使用物理信息神经网络（PINN）求解一维热传导方程，并蒸馏出其解析解。
*   **LLM 算术运算分析**：对 **Llama-3.2-1B** 执行加法、乘法、计数等任务的行为进行符号化建模，以揭示其内部计算逻辑和系统性偏差。

### 4. 资源与算力
*   **硬件使用**：
    *   **LLM 推理基准测试**：使用 **Nvidia A100-SXM4-80GB GPU**。
    *   **符号回归运行**：在 **Apple M4 Max SoC**（14 核 CPU，36GB 统一内存）上进行。
*   **训练/蒸馏时长**：
    *   LLM 的 MLP 块蒸馏：约 7-8 小时。
    *   GNN 边模型蒸馏：约 10 分钟。
    *   PINN 蒸馏：少于 3 分钟。
    *   LLM 算术操作分析：每个操作 5-15 分钟。

### 5. 实验数量与充分性
论文涵盖了从基础物理模拟（GNN/PINN）到前沿大语言模型（Transformer）的多种架构，实验设计较为全面：
*   **充分性**：不仅展示了方程恢复的准确性，还进行了 **PCA 敏感性分析**、**推理吞吐量测试**以及 **Pareto 前沿分析**。
*   **客观性**：通过与多种基准模型（如不同规模的开源 LLM）进行对比，证明了符号替代模型在“性能-速度”权衡中处于 Pareto 前沿，实验结果具有说服力。

### 6. 主要结论与发现
*   **自动化可行性**：SymTorch 成功解决了符号蒸馏的工程难题，实现了神经与符号组件的混合推理。
*   **物理发现**：在 GNN 实验中，通过剪枝和符号蒸馏，模型能够准确恢复隐藏在神经权重中的经验物理定律。
*   **LLM 加速**：通过将 LLM 的 3/28 层 MLP 替换为符号代理，在 Perplexity（困惑度）轻微上升的情况下，实现了 **8.3% 的推理吞吐量提升**。
*   **揭示偏差**：符号分析显示 LLM 在执行简单算术时并非进行精确计算，而是使用带有系统性误差的启发式近似方程。

### 7. 优点
*   **工程化程度高**：极大地简化了 SR 在深度学习中的应用流程，支持原生 PyTorch 的 `save/load` 和 `compile`。
*   **架构无关性**：方法通用于 GNN、CNN、Transformer 等多种结构。
*   **实用性强**：不仅用于解释，还探索了利用符号方程简化计算以加速推理的实际应用场景。

### 8. 不足与局限
*   **计算开销**：符号回归本质上是暴力搜索，其运行时间随变量数量和算子集大小呈指数级增长，处理极高维数据仍需依赖 PCA 等预处理。
*   **复杂函数拟合**：对于极度复杂的神经组件，SR 可能难以生成既准确又简洁的方程。
*   **泛化风险**：LLM 的符号替代实验主要在特定分布（Wikitext）上进行，其在分布偏移（OOD）下的表现尚未得到充分验证。
*   **解释性度量**：目前的复杂度度量仅基于表达式树的节点数，有时可能产生数学上等价但人类难以直观理解的复杂嵌套公式。

（完）
