---
title: An information-based model selection criterion for data-driven model discovery
title_zh: 一种用于数据驱动模型发现的基于信息论的模型选择准则
authors: "Michael C Chung, Alen Zacharia, Juan Guan"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.21393v1"
tags: ["keyword:SR", "query:SR"]
score: 9.0
evidence: 使用稀疏回归和新型信息准则从数据中提取可解释的符号模型。
tldr: 针对数据驱动模型发现中难以平衡拟合精度与模型稀疏性的问题，本研究提出了一种新型信息准则 SLIC。该方法结合了自动生成候选模型的稀疏回归算法，通过样本长度缩放对数信息准则来识别最优模型。实验证明，SLIC 在处理非线性微分方程及流体力学、纳米技术等实际实验数据时，其准确性和鲁棒性显著优于传统准则，为从复杂数据中提取可解释的物理模型提供了有力工具。
motivation: 现有的数据驱动模型发现算法在平衡拟合度与稀疏性时依赖人工调优，且容易出现过拟合和结果不稳定的问题。
method: 提出一种自动生成候选模型的稀疏回归算法，并引入样本长度缩放对数信息准则（SLIC）来自动筛选最优模型。
result: SLIC 在多种非线性微分方程的基准测试中表现优于流行信息准则，并成功从流体力学和纳米技术实验数据中提取出可解释模型。
conclusion: SLIC 为自动化、稳健的模型发现提供了新途径，能够有效从实验数据中产生具有科学预测价值的物理模型。
---

## 摘要
数据驱动模型发现 (DDMD) 算法是从数据中提取可解释符号模型的强大工具。然而，识别出最佳平衡拟合优度与稀疏性的模型通常是一个费力的过程，需要用户进行微调，且容易出现过拟合，其结果可能因模型初始化和特定训练过程而产生显著差异。在此，我们提出了一种稀疏回归算法，该算法能够自动且自适应地生成候选模型，并利用一种新型的样本长度缩放对数信息准则 (SLIC) 从这些候选模型中识别出最佳模型。我们证明了在从多个非线性常微分方程和偏微分方程的数据中提取正确模型方面，SLIC 的表现显著优于其他流行的信息准则。随后，我们展示了 SLIC 从流体动力学和纳米技术的实验数据集中发现可解释模型的能力，这些模型能够产生新的可测试预测。

## Abstract
Data-driven model discovery (DDMD) algorithms are powerful tools for extracting interpretable symbolic models from data. However, identifying the model that best balances goodness-of-fit and sparsity is often a laborious process requiring user fine-tuning, is prone to overfitting, and results may significantly vary depending on model initialization and specific training procedure. Here, we present a sparse regression algorithm that automatically and adaptively generates candidate models, and uses a novel sample-length-scaling logarithmic information criterion (SLIC) to identify the best model from these candidates. We demonstrate that SLIC greatly outperforms other popular information criteria in extracting the correct model from the data of several nonlinear ordinary and partial differential equations. Then, we demonstrate SLIC's ability to discover interpretable models from experimental datasets in fluid dynamics and nanotechnology that generate new testable predictions.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SLIC**（Sample-Length-scaling logarithmic Information Criterion，样本长度缩放对数信息准则）的新型模型选择方法，旨在解决数据驱动模型发现（DDMD）中难以平衡模型精度与稀疏性的核心挑战。

以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在从数据中提取数学方程（如微分方程）时，如何自动识别出既能准确拟合数据又足够简洁（稀疏）的模型？
*   **研究动机**：
    *   **现有准则的缺陷**：传统的模型选择准则（如 AIC, BIC）在数据量 $n$ 增大时，拟合优度项的权重会远超稀疏惩罚项，导致在大数据集上极易过拟合，引入多余的数学项。
    *   **人工干预过多**：现有的稀疏回归算法通常需要用户手动调节稀疏阈值（超参数 $\lambda$），这在处理实时数据或缺乏先验知识的系统时非常困难。
*   **整体含义**：论文提出了一套自动化的框架，通过 SLIC 准则和自适应阈值算法，实现了无需人工干预的稳健物理模型发现。

### 2. 方法论
*   **核心思想**：基于缩放和对称性原则，设计一个随样本量 $n$ 缩放且对精度和稀疏性进行对等惩罚的函数。
*   **SLIC 公式**：
    $$SLIC(\hat{\epsilon}, k) = n \log(\hat{\epsilon} k)$$
    其中，$n$ 是观测点数量，$\hat{\epsilon}$ 是均方误差（MSE），$k$ 是模型复杂度（非零参数的数量）。
*   **关键技术细节**：
    *   **方差最小化**：论文证明了最小化 SLIC 等价于最小化模型方差（$\hat{\epsilon} k / n$），从而在偏置-方差权衡中找到最优平衡点。
    *   **自适应稀疏回归算法**：
        1.  利用伪逆获取初始模型估计。
        2.  将模型系数的绝对值作为候选阈值。
        3.  迭代剪枝并使用 SLIC 进行评分，自动选出得分最低（最优）的模型。
    *   **弱形式（Galerkin）预处理**：为了应对噪声，将微分方程转化为积分形式（弱形式），避免直接对噪声数据求导。

### 3. 实验设计
*   **数据集/场景**：
    *   **模拟常微分方程 (ODEs)**：包括 Lorenz 系统、Rössler 吸引子、Lotka-Volterra 模型、Brusselator、Van der Pol 振子和非线性摆。
    *   **模拟偏微分方程 (PDEs)**：包括 Burgers 方程、KdV 方程、Kuramoto-Sivashinsky (KS) 方程、非线性薛定谔方程 (NLS) 和 Sine-Gordon 方程。
    *   **真实实验数据**：矩形水箱中的流体晃动（动力学预测）和 RNA-脂质体纳米颗粒的聚集过程（动力学核函数发现）。
*   **Benchmark（对比方法）**：对比了 AIC、AICc、BIC、HQIC、KIC 和 Bridge Criterion (BC) 等 6 种主流信息准则。
*   **评估指标**：准确率（Accuracy）、误报率（FPR，即错误包含项的比例）和缩放平均绝对误差（SMAE）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号或数量）。
*   **软件实现**：算法基于 **Julia 语言**实现，使用了 `OrdinaryDiffEq.jl` 进行仿真，`DiffEqFlux.jl` 进行参数估计，以及 `Optimization.jl` 进行优化。考虑到稀疏回归的计算复杂度较低，该方法通常在普通工作站上即可运行，无需大规模集群。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对每种噪声水平（0% 到 40%），每个系统都进行了 **25 次随机噪声实例化**的重复实验。
    *   进行了针对采样频率、轨迹长度、数据下采样和参数变化的鲁棒性测试。
*   **充分性评价**：实验设计非常充分且客观。它不仅覆盖了经典的基准数学模型，还跨越了从简单 ODE 到复杂高维 PDE 的维度，并最终在两个完全不同的真实物理实验领域验证了泛化能力。

### 6. 主要结论与发现
*   **卓越的鲁棒性**：SLIC 在所有测试的 ODE 和 PDE 系统中，识别正确项的准确率显著高于 AIC/BIC 等准则，尤其在高噪声环境下表现稳健。
*   **极低的误报率**：SLIC 几乎不会引入多余的物理项（FPR 接近 0），而传统准则在数据量大时倾向于产生极其复杂的过拟合模型。
*   **强大的预测能力**：
    *   在流体晃动实验中，仅用无驱动数据训练的模型成功预测了有驱动条件下的滞后和不稳定行为。
    *   在纳米技术实验中，SLIC 发现的“常数核”模型准确预测了稀释后的颗粒生长速率。

### 7. 优点
*   **理论优雅**：SLIC 的形式简洁，且与统计学中的方差最小化有直接联系。
*   **完全自动化**：消除了 DDMD 中最令人头疼的超参数调优问题，实现了“数据输入，模型输出”。
*   **数据量不变性**：解决了传统准则随数据量增加而失效的悖论，使模型发现结果对数据规模具有不变性。

### 8. 不足与局限
*   **库依赖性**：该方法仍依赖于用户预定义的非线性函数库（Library），如果真实的物理项不在库中，算法无法发现它。
*   **噪声极限**：虽然优于其他方法，但在极端噪声（如 >40%）下，任何基于回归的方法都会面临失效风险。
*   **理论深度**：论文主要从缩放和对称性角度推导 SLIC，虽然提及了与 KL 散度的潜在联系，但尚未建立严密的贝叶斯或信息论框架下的形式化证明。

（完）
